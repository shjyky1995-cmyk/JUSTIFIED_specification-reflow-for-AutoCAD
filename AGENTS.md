# Agent 开发规则
- 使用中文沟通与记录；本文件只保存长期规则，目标不超过 60 行。
- 首个目标：先打通可演示的 Word→解析→换行→分栏续页→DBText 落图闭环，再完成正式发布验收；不以延期打印检查反复阻塞主线。
- 设计人员的正式操作是每次用 CAD 小界面选择本次图幅/模板版本与 DOCX，再点一次位置；上次选择可预填，同图允许混用。开发用的多步提问不得留成正式流程。无警告不再追问；未知单位不猜，不按文件名换标准。
- 产品基线：[PRD](CAD_DesignNote_PRD_V1.0.md)；不得擅自扩大 V1 范围。
- 开始顺序：检查 Git 状态 → 阅读 [任务清单](docs/TASKS.md) → [架构](docs/ARCHITECTURE.md) → [固定流程](docs/WORKFLOW.md) / [当前接续](docs/NEXT_AGENT.md) → 当前任务日志、相关 PRD 条款及接口/测试。
- 按大阶段交付（见 WORKFLOW）；阶段内自主连续完成实现、修复、检查与提交，不为微小步骤反复要求用户确认；只在阶段完成、真正阻塞或必要讨论时交还。
- 用户说“继续下一步/开始下一步”：按 NEXT_AGENT 下一大阶段或中断位置直接接续；有失败先修、有 checkpoint 先恢复；具体验收语义见 WORKFLOW，不虚构未做的检查。
- 内部任务切换、阶段交付或中断时更新 NEXT_AGENT 的状态、用户待办、失败/延期项和下一阶段/恢复动作；日志追加历史，换 Agent 不依赖聊天记忆。
- 默认串行接续；并行必须使用独立分支和 worktree，不同时修改同一工作目录。
- 任务分支使用 task/<编号>-<简称>；main 只接收已通过该任务检查的结果。
- 遵守架构依赖方向；AutoCAD API 仅允许在 AutoCadAdapter、PluginHost 中出现。
- 共享协议、模块依赖、技术栈变更：先记录原因、影响及方案，由用户确认后实施。
- 模块内部实现、修复及必要测试可自主完成，不反复请求许可。
- 不预建 Future 空服务；不实现 MText 输出、Word 视觉排版继承、CAD 增量追踪。
- 项目自行制定完整院标；支持多套机构/个人标准独立配置和修改，格式与模板分开版本化、参数不写死；见 [格式](docs/TEXT_FORMAT_V1.md) / [模板](docs/PAPER_TEMPLATES_V1.md)。
- 未验证项保持 pending；区分已制定规则与实测证据，生产模板禁止以测试值或静默默认值代替。
- 每个可验证的小步：完成相关检查 → 追加任务日志 → Git Commit，不能只改文件不提交。
- 提交格式：feat(T03): 描述；可使用 fix/test/docs/build/chore 等类型。
- 仅暂存本任务文件；不覆盖他人改动，不擅自 reset --hard、强推或重写历史。
- 中断前将本任务未完成改动提交为 chore(Txx): checkpoint ...；写明失败项，不合入 main。
- 日志追加保留历史，记录原因、改动、测试、问题和下一步；通过任务编号查询提交 SHA。
- 更新任务状态：待开始 / 进行中 / 阻塞 / 待验收 / 完成；缺真实证据不得标记完成。
- 核心测试无需 CAD；模拟测量测试不能替代真实宿主、字形及打印核验。
- 宿主/桌面操作默认交给用户在方便时执行；Agent 先备好简短步骤、样本和自动报告，继续不依赖人工的工作。仅用户明确表示不方便并让 Agent 代操作时使用 Computer Use，不自行反复截图轮询消耗额度。
- SDK、字体、私有业务文件、密钥、构建产物不默认入库；只提交脱敏样本。
- 阶段满足验收条件后合并；依赖未验收祖先的分支不得直接带入 main；阶段验收后打标签并推送私有仓库，交付如实说明合并/备份状态。
- 回滚默认 git revert，保留历史；操作步骤见 [开发指南](docs/DEVELOPMENT.md)。
- 阶段结束或中断时按 WORKFLOW 固定模板交付：完成内容、检查/提交、用户操作、通过标准、异常反馈、未完成项、唯一下一步；无人工作业时明确“无需操作，回复继续下一步”。
