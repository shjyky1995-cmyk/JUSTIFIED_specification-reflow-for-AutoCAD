# Agent 开发规则
- 使用中文沟通与记录；本文件只保存长期规则，目标不超过 60 行。
- 产品基线：[PRD](CAD_DesignNote_PRD_V1.0.md)；不得擅自扩大 V1 范围。
- 开始顺序：检查 Git 状态 → 阅读 [任务清单](docs/TASKS.md) → [架构](docs/ARCHITECTURE.md) → 当前任务日志及相关 PRD 条款。
- 一次领取一个可独立验收任务；先明确编号、输入输出、依赖、允许修改范围及 REQ/AC。
- 默认串行接续；并行必须使用独立分支和 worktree，不同时修改同一工作目录。
- 任务分支使用 task/<编号>-<简称>；main 只接收已通过该任务检查的结果。
- 遵守架构依赖方向；AutoCAD API 仅允许在 AutoCadAdapter、PluginHost 中出现。
- 共享协议、模块依赖、技术栈变更：先记录原因、影响及方案，由用户确认后实施。
- 模块内部实现、修复及必要测试可自主完成，不反复请求许可。
- 不预建 Future 空服务；不实现 MText 输出、Word 视觉排版继承、CAD 增量追踪。
- 未标定值保持 pending；生产模板禁止以测试值或静默默认值代替。
- 每个可验证的小步：完成相关检查 → 追加任务日志 → Git Commit，不能只改文件不提交。
- 提交格式：feat(T03): 描述；可使用 fix/test/docs/build/chore 等类型。
- 仅暂存本任务文件；不覆盖他人改动，不擅自 reset --hard、强推或重写历史。
- 中断前将本任务未完成改动提交为 chore(Txx): checkpoint ...；写明失败项，不合入 main。
- 日志追加保留历史，记录原因、改动、测试、问题和下一步；通过任务编号查询提交 SHA。
- 更新任务状态：待开始 / 进行中 / 阻塞 / 待验收 / 完成；缺真实证据不得标记完成。
- 核心测试无需 CAD；模拟测量测试不能替代真实宿主、字形及打印核验。
- SDK、字体、私有业务文件、密钥、构建产物不默认入库；只提交脱敏样本。
- 任务检查通过后合并；阶段验收后打标签并推送 GitHub 私有仓库；未配置远程必须说明未备份。
- 回滚默认 git revert，保留历史；操作步骤见 [开发指南](docs/DEVELOPMENT.md)。
- 结束必须报告：完成内容、检查结果、提交编号、阻塞、下一任务。

