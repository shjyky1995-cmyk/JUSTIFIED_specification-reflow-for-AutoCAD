# 标定与环境台账
文字格式参数已按用户授权制定，见 [项目格式 V1](TEXT_FORMAT_V1.md)。当前尚无完成全部验证的可发布标准包或 A1/A2/A3 模板；规则冻结与验证状态分开记录。
测试 fixture 必须明确标为测试，禁止放入正式标准包。
CAL 完成需记录样本身份/hash、值、单位、验证证据、复核人和冻结版本。

| 编号 | 内容 | 当前证据 / 待补内容 | 状态 |
| --- | --- | --- | --- |
| CAL-01 | CAD/位数/OS/空间/单位 | 本机 AutoCAD 2021 24.0.47.0、Windows x64；2026-09-22 手工 NETLOAD 与 DN_DIAG 通过；空间单位和外机验收待补 | pending |
| CAL-02 | 字体/大字体/字高/宽度/倾角/图层 | 字体身份已读取；项目规则已定：tssdeng+tssdchn、4.5 mm、0.75、0°、JSR_NOTE_TEXT；真实宿主输出已验证（6 个 DBText 属性核对，见 T10 日志）；打印核验移至 T12 一次性确认 | 规则已定，输出已验证；打印移 T12 |
| CAL-03 | 标题正文编号缩进/行距 | 项目规则已定：统一字高、7.2 mm 行距、首行/续行起点 0、编号内联两个半角空格、标题无额外槽 | 规则已定，验证 pending |
| CAL-04 | A1/A2/A3 栏与边界 | 三图幅规则已制定：A1/A2三栏、A3两栏，见 PAPER_TEMPLATES_V1.md | 规则已定，验证 pending |
| CAL-05 | 多页方向/间距/图框 | A1/A2/A3向右步长861/614/440 mm，framePolicy=none，属于项目决策；多页图面已验证（平移(100,50)与UCS宿主核对，见 T10 日志） | 规则已定，已验证 |
| CAL-06 | Word/WPS 样式与编号 | T04 同义核心样本已核对；T12 无表格 WPS 工程说明在 AutoCAD A1 生成 219 个 DBText、2 页，见 T12 日志；行边界可折叠空格复核见同日志 | 完成（输入语义与 A1 宿主证据） |
| CAL-07 | 字符/上下标/公差 | 上下标标定由项目制定：缩放 0.7、上标抬升 0.35、下标下沉 0.2（相对字高，见 TEXT_FORMAT_V1.md）；字符清单 20～50 句宿主核对与上下标图面核对进行中；打印字样已移至 T12 | 规则已定，验证进行中 |
| CAL-08 | 性能/硬件/取消 | T11 本机合成 1 万字 20 次热运行点选后 P95=373ms；处理中取消已成功一次。真实业务样本、设备台账和取消时延 P95 待补 | pending |
| CAL-09 | 文件/解压/页数/对象/耗时上限 | T12 13 份原始说明扫描：源文件最大 71,397 B、ZIP 展开总量最大 1,135,434 B、正文 w:t 最大 32,744 UTF-16 单元；现行块/字符/页/实体限额见 GenerationLimits。源文件/展开/耗时上限仍未标定，不能把样本最大值当发布阈值 | pending |

本机安装路径仅为本次环境记录：E:/Program Files/Autodesk/AutoCAD 2021。
构建不得硬编码此路径；从参数或本机注册表发现。
资料存放 local/ 或 private/（不入库），可入库的仅为经确认脱敏且可分发的样本。

## 2026-09-22 A1 现状证据补充
已读取用户提供《新块.dwg》，详情见 [T06 A1 实测](calibration/T06-A1-新块-实测.md)。已获得字体路径、混合文字属性、三栏几何及行格数；单位无单位，实际正文基线距与行格距不一致，打印设置与图框比例标记不一致。以上是测量证据，尚非冻结的生产参数，表内各 CAL 状态仍为 pending。真实正文、原始对象快照及字体身份 hash 仅存 local/calibration，不入库。

## 2026-09-22 用户授权自行制定格式
本次决定取代前文中等待院内统一字高/行距/缩进的要求；历史实测数值仍保留，不反向修改。CAL-02/03 的格式规则已定，不等于宿主字形、上下标、模板几何和打印已通过。T06 下一步直接使用项目格式实施和验证，不再要求用户找齐完整院标。

## 2026-09-22 验收范围调整（用户确认）

删除项：独立打印标定关卡（打印核验并入 T12 最终验收做一次真实打印确认；DBText 为 CAD 原生实体，插件无可标定的打印参数）；完整符号映射表构建（符号映射以 passthrough-1.0.0 为发布值，以字符清单核对结果为准）。保留项：上下标标定与验收（PRD V1 有限输入契约：有标定按缩放与基线偏移生成，缺标定阻断，不扁平化）。CAL-02/05/07 相应更新，其余 CAL 状态不变。
