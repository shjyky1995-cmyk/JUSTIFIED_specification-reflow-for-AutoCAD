# T10 — Word 到 CAD 命令

## 2026-09-22：领取

- 输入：T09 的放置和写入。REQ-CAD-002、REQ-UX-001，AC-11、AC-12。
- 输出：命令 `DN_NOTE`。解析、换行、放置都走已有端口；写图在文档锁里做一次事务。
- 范围：PluginHost、Standards 的草案内存读取、Application 的生成编排、测试说明。不改草案文件，不降低安全设置。

## 2026-09-22：命令

- `DN_NOTE` 选择 DOCX、drafts 目录、A1/A2/A3 和必须手输的单位比例，再点一次说明区右上角。拾取点按当前 UCS 转到世界坐标，文字旋转保持 0。
- 草案的 pending 只在内存里补成可计算的值，并打印 DN_NOTE_DRAFT。磁盘上的 json 不变，正式目录仍然拒绝草案。
- 解析或排版错误不写图。警告先显示，要输入「是」才写入。第二次生成不删除旧字。一次 U 撤销本次提交。
- 演示样本是「测试文件/示例-单行说明.docx」。它没有上下标。「示例-结构说明.docx」含上下标，命令会整篇拒绝。
- 检查：同上，net8.0 与 net48 各 117/117，插件编译 0 警告。图面、撤销和 UCS 还要人在 AutoCAD 里执行 DN_NOTE 后才算通过。
- 状态：待验收。阶段 2 到此交付。

## 2026-09-22：第一次图面写入失败

- 用户在空白图执行 DN_NOTE。解析、草案提示、1 页 6 个对象、字高 4.5、宽度系数 0.75 都正确。锚点世界坐标 (28.7066, 12.9771) 加上 A1 首栏左 -710、首基线 -6.2 后，第一行是 (-681.2934, 6.7771)，与 DN_NOTE_FIRST 一致。
- 写入时报 `E_RENDER_FAILED Exception`，实体数 before=0 after=0，事务已回滚。异常类型名是 AutoCAD 的 Exception，原先没把 ErrorStatus 打出来。
- 原因：左对齐、基线对齐的 DBText 又设置了 AlignmentPoint 并调用 AdjustAlignment，2021 会抛 eInvalidInput。测量命令能成功，是因为它只设置 Position。样式改为写入字体文件名 tssdeng.shx / tssdchn.shx，不再把 FindFile 的完整路径写进样式。
- 新程序在 artifacts/dn-note-2。旧的 artifacts/dn-note 正被这次 AutoCAD 加载，不能覆盖。
- 用户指出当前多步提问不能成为设计人员的正式流程。正式操作应接近一键：标准、图幅和单位比例设一次，之后一个命令加点选位置即可生成。本次不改命令，只记入 NEXT_AGENT。

