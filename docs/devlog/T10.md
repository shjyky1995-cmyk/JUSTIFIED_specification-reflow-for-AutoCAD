# T10 — Word 到 CAD 命令

## 2026-09-22：领取

- 输入：T09 的放置和写入。REQ-CAD-002、REQ-UX-001，AC-11、AC-12。
- 输出：命令 `DN_NOTE`。解析、换行、放置都走已有端口；写图在文档锁里做一次事务。
- 范围：PluginHost、Standards 的草案内存读取、Application 的生成编排、测试说明。不改草案文件，不降低安全设置。

## 2026-09-22：命令

- `DN_NOTE` 选择 DOCX、drafts 目录、A1/A2/A3 和必须手输的单位比例，再点一次说明区右上角。拾取点按当前 UCS 转到世界坐标，文字旋转保持 0。
- 草案的 pending 只在内存里补成可计算的值，并打印 DN_NOTE_DRAFT。磁盘上的 json 不变，正式目录仍然拒绝草案。
- 解析或排版错误不写图。警告先显示，要输入「是」才写入。第二次生成不删除旧字。一次 U 撤销本次提交。
- 演示样本是「测试文件/示例-单行说明.docx」。它没有上下标。「示例-结构说明.docx」含上下标，命令会整篇拒绝。
- 检查：同上，net8.0 与 net48 各 117/117，插件编译 0 警告。图面、撤销和 UCS 还要人在 AutoCAD 里执行 DN_NOTE 后才算通过。
- 状态：待验收。阶段 2 到此交付。

## 2026-09-22：第一次图面写入失败

- 用户在空白图执行 DN_NOTE。解析、草案提示、1 页 6 个对象、字高 4.5、宽度系数 0.75 都正确。锚点世界坐标 (28.7066, 12.9771) 加上 A1 首栏左 -710、首基线 -6.2 后，第一行是 (-681.2934, 6.7771)，与 DN_NOTE_FIRST 一致。
- 写入时报 `E_RENDER_FAILED Exception`，实体数 before=0 after=0，事务已回滚。异常类型名是 AutoCAD 的 Exception，原先没把 ErrorStatus 打出来。
- 原因：左对齐、基线对齐的 DBText 又设置了 AlignmentPoint 并调用 AdjustAlignment，2021 会抛 eInvalidInput。测量命令能成功，是因为它只设置 Position。样式改为写入字体文件名 tssdeng.shx / tssdchn.shx，不再把 FindFile 的完整路径写进样式。
- 新程序在 artifacts/dn-note-2。旧的 artifacts/dn-note 正被这次 AutoCAD 加载，不能覆盖。
- 用户要求按这个目标改项目要求，不能只记在交接里。已改 PRD 3.3 与 AP-004、AGENTS、WORKFLOW、PAPER_TEMPLATES：工程设置一次，日常一个命令点一次位置生成。当前 DN_NOTE 降为开发核对。本次没有改命令实现。

## 2026-09-22 15:08：Agent 接管宿主重试，加载安全提示阻塞

- 用户要求 Agent 代做 CAD 测试，根据真实返回结果继续开发。读取接续后工作区干净，分支 task/T09-dbtext，修复基线 a46c5b2。
- 使用 computer-use 启动独立 AutoCAD 2021 实例，新建空白 Drawing1，保留用户原实例与图纸。通过 NETLOAD 加载 artifacts/dn-note-2/Justified.SpecificationReflow.AutoCAD.PluginHost.dll。
- DLL SHA256：0575F6F2CF3EA981F2B40615128A382DBB1B965A17ADA292B764DA7A2D816D37。
- 实际屏幕出现“安全性 - 未签名的可执行文件”，提示未知发布者、不在受信任文件夹。尚未加载成功，尚未执行新版 DN_NOTE。computer-use guidance 明确禁止操作安全许可提示，已停在该弹窗并请用户亲自点击“加载一次”。未降低安全设置，未搬移 DLL 绕过安全检查。
- 独立补跑 scripts/build.ps1 -Target Core：0 警告、0 错误；net8.0 与 net48 各 117/117，通过、0 跳过。TRX 在 artifacts/test-results，时间分别为 15_08_07、15_08_11。此检查不覆盖宿主写入器，不能证明修复已通过图面测试。
- 中断状态：T09/T10 仍待验收，当前宿主测试被加载许可阻塞；未启动阶段3开发、未合并、未打标签、未推送。
- 唯一恢复动作：用户亲自处理当前加载提示后，Agent 在新实例完成样本生成、图面/DBText 类型、再次生成与一次撤销检查，记录证据后按结果修复或继续阶段3。无需用户重新执行完整测试流程。

## 2026-09-22 15:15：新版真实 AutoCAD 2021 重试通过

- 用户亲自处理插件加载提示后，由 Agent 在独立实例运行 a46c5b2 构建。样本为示例-单行说明.docx 的逐字节副本 artifacts/cad-retry/note.docx；未修改源文件、安全设置或生产配置。
- 首次 A1、比例1、WCS原点：DN_NOTE_OK objects=6，before=0 after=6；首行(-710,-6.2)，高4.5、宽度系数0.75。屏幕观察标题、中文正文、C30、HRB400、Φ20@200、35mm、续行与显式空行均呈现；未将屏幕观察算作完整字形/打印标定。
- 第二次 Anchor=(100,50)：before=6 after=12，6个新实体均较第一次平移(100,50)；第一次实体的完整entget保持相同。一次独立 U 后剩6个，与第一次实体逐项完全一致。生成由原生命令脚本驱动，撤销日志为 GROUP DN_NOTE (LISP表达式)；日常新入口仍需独立验证。
- 数据库核对：全部为TEXT/AcDbText，字高4.5、宽度0.75、旋转0，图层JSR_NOTE_TEXT，无MText。
- UCS原点(100,200)，Z旋转90°，输入点(10,20)转换WCS(80,210)，比例2：首行(-1340,197.6)，字高9；6个实体坐标均满足(80,210)+2×原始坐标，旋转仍为0。
- 含上下标样本：E_TEMPLATE_INVALID；before=12 after=12，完整实体数据不变。另在点选位置处Escape：DN_NOTE_CANCELLED，12个实体数据不变。
- 原始命令日志、前后实体快照、数值核对checks.json及T10-host-evidence.dwg存于artifacts/cad-retry；日志为本机GBK编码。FILEDIA、LOGFILEMODE、LOGFILEPATH已恢复，UCS恢复世界。
- 当前最小落图重试关卡通过。T09/T10整体仍待验收（正式一键入口、组合字符/故障注入等未覆盖）；用户已授权无需再次人工操作即可继续开发。下一步实现已批准PRD3.3的一次设置与单次点选，随后衔接T11运行报告、限额和部署。T06打印/完整标定及外机业务验收仍pending，不合main、不打标签、不推送。

## 2026-09-22 15:5x：正式一键入口落地（PRD 3.3）

- 新增 `DN_NOTE_SET`：一次设置标准包根目录、图幅、模板序号、单位比例、说明文档和可选报告目录，写入当前图 JSR_NOTE_SETTINGS 命名字典（Xrecord 存 Base64 分块 JSON，编解码在 Standards.NoteSettingsCodec，图内存储在 AutoCadAdapter.DrawingSettingsStore）。只接受 classification=production 且已标定的包；草案目录拒绝并提示改用 DN_NOTE_DEV。
- `DN_NOTE` 改为正式日常命令：读图内设置→校验文档存在→点一次位置→按设置重新加载生产包并核对 id/version 与设置一致→生成→落图。无警告不追问；有警告才问是/否；取消或失败零新增；一次 U 撤销。生成限额默认 MaxPages 200 / MaxTexts 4000 / MaxBlocks 500 / MaxCharacters 200000，超出以 E_RESOURCE_LIMIT 拒绝，不截断。报告目录非空时写 dn-note-<时间>.json 运行报告（页数、对象数、告警、文字包围盒、耗时）。
- 原多步命令更名为 `DN_NOTE_DEV`，日志前缀改 DN_NOTE_DEV_*，草案 pending 内存补齐行为不变，正式人员不用它。
- 检查：核心测试 net8.0 与 net48 各 131/131，0 警告、0 错误；插件编译 0 警告。新测试覆盖设置往返与损坏拒绝、Problems 自检、四个限额触发点、限额内正常生成、运行报告字段、模板枚举与取消。
- 宿主验证未完成：Core Console 拒绝加载未签名插件（SECURELOAD，无 GUI 无法点“加载一次”）。已备本机生产测试包与脚本 artifacts/cad-retry-note3/（published 包从 drafts 派生、note3-main.scr 覆盖设置→生成→撤销→平移→UCS比例2，note3-nosetting.scr 覆盖无设置拒绝、草案目录拒绝、A2 无模板拒绝、DN_NOTE_DEV 回归）。需要用户在独立实例点一次“加载一次”后执行。
- 状态：T10 仍待验收（正式命令的图面证据未取得）。不合 main、不打标签、不推送。
