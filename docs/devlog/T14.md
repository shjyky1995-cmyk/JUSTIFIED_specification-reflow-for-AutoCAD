# T14：CAD 内每次选择本次说明

## 2026-09-25：家中截图后的窗口压缩、免选模板与 CAD 新建弹窗

- 用户截图 `测试结果/0925/YMFY.png` 显示旧窗口约 1000×690、字体和卡片偏大，不能拖动，而且未预装模板导致继续提示选择目录。用户要求这轮由 Agent 完成后再通知测试。
- 把窗体固定为 800×550，基础字 14 像素，标题 23、字段 16、图幅主字 19；页眉空白、Logo、标题和副标题可拖动。输入框和按钮放入各自容器以固定层次，避免实机遮挡。移除模板目录按钮，固定从插件同目录 `standards/published` 取唯一的 A1/A2/A3 模板，缺失/冲突时提示管理员。无单位时仍不猜测。
- Release 包构建 0 警告、net8.0/net48 各 144/144、22 文件核验通过。WinForms 离线预览 `artifacts/t14-ui-preview.png` 已目视核对，大小 800×550、Logo 和各控件无遮挡；真实 CAD 截图仍 pending。
- 当前用户 ApplicationPlugins bundle 已更新；旧版备份为 `.bundle.backup-20260925-161538`，安装的 PluginHost DLL SHA256 `1CD0213F957E97B70C7C2941CBEFFE20FE29C30760F3F57D15E85C5D2CC82933`。仅在**本机已安装测试包**的 `Contents/Windows/standards/published` 中加入原有三图幅 local-test-only 资料和标识，仓库生产 `standards/published` 仍为空，候选 `productionReady=false`。未上传云盘。
- AutoCAD 2021 实际会话：原读取 `FILEDIA=1`、`CMDDIA=1`、`STARTUP=3`。参考 Autodesk 对 `STARTUP` 的定义，设置为 1，回读 `FILEDIA=1 CMDDIA=1 STARTUP=1`；只关闭 Agent 新建且 `DBMOD=0` 的 Drawing1 后部署。部署后 CAD 已再次启动，电脑界面工具空列表、COM 尚未注册，未直接观察新建对话框或新插件运行；需用户按 `docs/T14_HOME_TEST.md` 实测。新建弹窗问题不应再归因于 FILEDIA 当前为 0。
- 代码与本轮说明提交 `3bc6c4c`。提交后重建的可追溯候选 ZIP 位于 `artifacts/packages/20260925-161742-243/JUSTIFIED_specification-reflow-for-AutoCAD-0.1.0-candidate-3bc6c4c.zip`，SHA256 `A97B142BB1238A35A2FE66294702C0F5C5FC844A063C838B6667E9287A3E44DB`；重建后 DLL SHA 为 `D03E322CA2D5F45CD7736214783A04F621C63C4638629D099DE5BAC5060B3207`。已安装的同代码先前构建 DLL SHA 仍为 `1CD0213F957E97B70C7C2941CBEFFE20FE29C30760F3F57D15E85C5D2CC82933`，因为 CAD 在重启后的开始页未响应关闭请求，未热替换正在加载的 DLL。两者代码相同，构建元数据不同；用户测试以已安装 DLL 为准。原夹具换行差异及用户测试资料保持未暂存。下一步仅为本机 CAD 复测与回传，不能把离线预览当真实宿主通过。

## 2026-09-24：需求与实现

- 负责人：当前 Agent。输入：用户要求同一 DWG 可先后导入不同图幅、模板版本和 Word 说明；现有 T10/T11 正式排版落图链路。输出：`DN_NOTE` 小窗口和明确复用上次选择的 `DN_NOTE_REPEAT`。
- 选择窗口复用 WinForms（架构已允许），从 `DirectoryPackageCatalog` 列出 production/calibrated 模板，先选图幅，再选模板 ID/版本；选择本次 DOCX 并填写已核对的单位比例。上次选择仅预填。提交前仍走正式 `Load` 校验，模板摘要与实物图幅不一致时拒绝。
- `DN_NOTE` 选择后更新图内最后一次选择，再调用原有生成链路。一次生成不会删除先前的说明；用户下次可重新选择同图的另一图幅/Word。`DN_NOTE_REPEAT` 保留不打开窗口的快捷路径。原 `DN_NOTE_SET` 保留给旧图与 T12 验收脚本，原始 T12 私有转移 ZIP 不含新窗口。
- 当前 `PaperCode` 契约和模板 enum 仍只支持 A1/A2/A3。窗口从目录列示图幅是为了避免 UI 写死图幅；未来新增图幅仍需模型/模板/字体与宿主验收，不能只放一个 JSON 文件就宣称可用。
- 检查：`scripts/build.ps1 -Target All` 通过，0 警告；net8.0/net48 核心测试各 143/143。尚未进行真实 AutoCAD 2021 窗口交互、取消、同图混合落图检查，状态进行中。
- 代码提交 `e26f8eb`。随后 `scripts/build.ps1 -Target Package` 再次通过双框架各 143/143，生成独立候选 ZIP：`artifacts/packages/20260924-083746-892/JUSTIFIED_specification-reflow-for-AutoCAD-0.1.0-candidate-e26f8eb.zip`，SHA256 `AE7EEC6D2268792BAE921072D35680D603E8D4E2FF6441FC6CBF41978B66D912`；bundle 清单 22 文件通过。BUILD.json 的 `workingTreeDirty=true` 是工作区原有、未暂存的测试夹具换行差异，不能据此宣称产物来自完全干净的工作区；包内仅含已提交的 T14 编译内容。
- 修正现存 PRD/安装说明的冲突文案和 `DN_NOTE_SET` 成功提示后，提交 `7fb5884`，再次构建独立候选 ZIP：`artifacts/packages/20260924-083931-125/JUSTIFIED_specification-reflow-for-AutoCAD-0.1.0-candidate-7fb5884.zip`，SHA256 `FDA77CE7B7ABB8DEA31A09DB950B8F69B75663FD717BC85DB9A9BDF85612D41A`。双框架各 143/143、0 警告、包 22 文件清单通过；前一 ZIP 留作历史，以本条候选为准。原有测试夹具换行差异仍未暂存，BUILD.json 如实标记工作区有改动。
- 发现 BUILD.json 的待验清单未列 T14/T13，提交 `bc3cb48` 修正。最新候选包为 `artifacts/packages/20260924-084110-671/JUSTIFIED_specification-reflow-for-AutoCAD-0.1.0-candidate-bc3cb48.zip`，SHA256 `D368945B1E898895A7449F9231C1EF268123B07D886FDA3759B4E8F8D6CE2B84`；双框架各 143/143、0 警告、22 文件校验通过。此前两个 ZIP 留作历史；只用本条候选验证 T14。BUILD.json 仍因原有、未暂存的夹具换行差异标记工作区有改动，未伪报干净。

## 宿主待验

1. 用独立候选包加载 AutoCAD 2021，在空白图执行 `DN_NOTE`，查看图幅与模板版本是否来自已发布目录；选择 A1 和第一份 DOCX 后点位生成。
2. 再执行 `DN_NOTE`，改选 A2 或 A3 与另一 DOCX，点另一处。观察两组 DBText 均保留、各自分栏续页与图幅对应；再次打开窗口应预填上次选择，但仍可改选。
3. 取消窗口与取消点位分别检查没有新增文字；输入不存在的 Word、无效单位或无效模板应明确报错且不落图。记录命令输出、运行报告和图面截图；未实测不标通过。

## 2026-09-24：供用户外机试验的私有材料

- 用户请求新版测试步骤及发往本人 Gmail，并确认可随包携带其业务样本。已在忽略入库的 `artifacts/t14-share/T14-CAD选择窗口-测试包.zip` 组合最新版 T14 候选插件、T12 本地测试标准、`sample.docx` 和中文操作说明；ZIP SHA256 `8491E9B210334388034691EE1FCACF4CCB9E0B75E064367508F47B3D9CF05DB9`，重新解压后 8 个文件逐项 SHA256 检查通过。业务样本未提交或上传。
- 未发送邮件：当前无可用 Gmail 连接，连接请求两次未能验证；且 Gmail 官方明确拦截 `.dll` / `.ps1` 等文件及包含它们的 ZIP。不能声称“已寄送”。用户可从本机复制 ZIP 到获授权的测试电脑；若以后用受控文件共享发送，须保留业务资料访问限制。

## 2026-09-24：单位电脑回传与体验问题

- 用户回传忽略入库的 `测试结果/0924/命令行提示.txt`、`界面截图.png` 和 `BUILD.json`。BUILD 来源为 `bc3cb48` 候选包；真实 AutoCAD 命令记录显示 `DN_DIAG_OK`，同一图中连续运行 `DN_NOTE`：A1 2 页/219 对象、A2 2 页/262 对象、A3 5 页/255 对象，累计 736 对象，三次 warnings=0。第四次窗口取消得到 `DN_NOTE_CANCELLED`。截图是生成后的 CAD 图面，不是选择窗口。只确认这些观察到的宿主结果；不同 DOCX、点位取消、打印、断网与图面专业核对仍 pending。
- 用户反馈窗口外观不满意，并表示点位后生成耗时长。当前回传没有选择窗口截图或秒数，不能给出耗时数值或断言瓶颈。决策：保留闭环进展，先用分段计时测出原因，再针对占比最大的阶段优化并外机复测；窗口改版在 T14 宿主验收前完成，二者均不得延期到普通用户正式发布之后。T13 安装体验可做不依赖这些结果的准备，但不能因此把 T12/T14 标为完成。
- 排版服务当前通过 `HostTextMeasureService` 调用真实 CAD 字宽；每次未命中引擎缓存的请求会开事务、建临时样式和文字，再回滚。这是待测的性能嫌疑点，不是已经证实的根因。新增命令行 `DN_NOTE_TIMING_MS`，记录点位后总耗时、读取/解析/排版/CAD 测量调用次数与耗时/坐标放置/写入分段，不改变生成逻辑。该行只在新包中出现，旧回传无法补算。`scripts/build.ps1 -Target All` 通过：0 警告，net8.0/net48 各 143/143；模拟测量结果不得充当 CAD 性能签收。
- 提交 `383d476` 后 `scripts/build.ps1 -Target Package` 再次通过：0 警告、双框架各 143/143、bundle 清单 22 文件通过。新候选插件位于 `artifacts/packages/20260924-153431-775/JUSTIFIED_specification-reflow-for-AutoCAD-0.1.0-candidate-383d476.zip`。为单位电脑最短复测组装私有 `artifacts/t14-share/T14-CAD计时复测包-383d476.zip`，含新插件、原业务样本和原本地测试标准，SHA256 `14FC43E378A454162622A174C639C0D1A7BAB04F1D5E26591D99C45CB2E779E0`；重新解压后 8 文件清单核验通过。包内 `先读我.txt` 只要求同 DOCX A1/A2/A3 暖启动生成并回传三行计时与选择窗口截图。私有业务样本未提交 Git；外机复测尚未执行。
- 用户随后确认 A2 从点下位置至全部生成约 21 秒。暂记为一次人工计时；旧/新插件版本及分段尚待确认，不填作 CAL-08 P95 正式统计。对交互体验已属严重延迟，性能优化列为正式推广前必须完成的 T12 验收项；新包的分段数据用于定位主因。
- 用户此前已授权携带业务样本到本人 Google 账号，并在本轮要求便于单位电脑取得包。已确认 Google Drive 连接账号为 `shjyky1995@gmail.com`，将上述私有计时复测包上传为该账号云盘文件 ID `1iDvzVPj6WsmCSmdEGpbh2px_muoiIbHq`；随后元数据回读确认文件名、ZIP 类型和大小 2,207,378 字节。云端访问权限状态未由接口证实，不称为公开共享。此包仍是旧窗口外观，专供计时定位；新版 UI 待设计与验收。
- 用户要求小白可照着做的详细说明。已把包内 `先读我.txt` 扩写为下载、双层解压、退出 CAD、备份并替换旧 `.bundle`、逐次选择 A1/A2/A3、按 F2 复制计时行、常见错误处理等 44 行，重新生成清单并解压核验 8 文件。就地更新同一 Drive 文件 ID，元数据回读确认新大小 2,209,247 字节；最终本地 ZIP SHA256 `DC61CBEAAB93C1E953E1B369E1CA04800744514AB294C47F4C26F85C4DB67FF2`。前述初版 ZIP 的 SHA256 与大小仅留历史，不用于本轮下载校验。
- 用户最新排序：CAD 小窗口的视觉改版等 Word→CAD 全流程、性能和图形化安装走通后再做；希望 CAD 插件与未来独立桌面客户端视觉统一，桌面端倾向 Electron。判断：此顺序可行，现有 PRD 已把独立桌面工作台放在 V1 以外；Electron 仅作为后续候选，不能直接代替 CAD 宿主测量与落图，也不能未经评估改变当前 C# 技术栈。已在 `docs/TASKS.md` 和 `docs/ARCHITECTURE.md` 写入分阶段提案。用户对 A2 约 21 秒的性能反馈仍优先解决；视觉延后不等于正式推广可不验收。未写桌面代码或新增文件。

## 2026-09-24：根据新截图做视觉概念

- 用户将第二批外机结果放在 `测试结果/0924(1)`，其中有当前 WinForms 输入窗口截图和一张暂定品牌 logo 草图；logo 中文名称尚未确定。应用户明确要求，制作极简、现代、图标辅助而不失可理解性的 CAD 小窗口概念图 `docs/assets/cad-note-picker-concept-v1.png`。图中的 logo 为依据草图的近似示意，不是最终商标定稿；概念图不是已实现 UI。具体交互与双端视觉复用原则已记入 `docs/TASKS.md`。窗口代码暂不改，仍按用户顺序先修真实测量性能及内容正确性，再做视觉落地。
- 用户反馈 V1：图幅要有“更多”供以后扩充；日常窗口不要暴露“标准包”；只留 Word、图幅、模板版本、单位比例；流程为“选择→点位置”，点完直接生成并关闭。已制作 `docs/assets/cad-note-picker-concept-v2.png`，V1 图片留历史；TASKS 更新为最新交互规则。该图仍是概念图，尚未改 CAD 窗口代码。多套标准支持仍保留在配置层，不能因窗口删去该字段而静默切换。

## 2026-09-25：V2 窗口与家中本地测试准备

- 用户停止 Google Drive 上传，表示已回家，要求本地测试或 Agent 预先部署，并同意按 V2 稿继续修改 CAD 页面。此前“视觉改版后做”的排序由此更新；T12 真实性能、打印等验收不自动通过。
- `NotePickerForm` 改为 V2 四项布局：DOCX、A1/A2/A3 图幅卡片和仅列实际模板的“更多”、模板版本、单位比例；页脚点位按钮。上次设置继续预填，目录从设置继承；缺目录时可点“选择模板目录”。模板列表附标准来源 ID，避免多套标准时静默混淆。未采用用户草图作为正式 logo，仅使用临时几何符号。
- `scripts/build.ps1 -Target All`：插件 0 警告、0 错误；net8.0/net48 各 144/144。核心测试不证明宿主窗口尺寸、高 DPI、焦点或真实性能。家中测试操作见 `docs/T14_HOME_TEST.md`。
- 待完成：打候选包并部署当前 Windows 用户的 ApplicationPlugins，记录旧版备份与候选 SHA；用户在 AutoCAD 2021 空白图观察新版窗口、计时、取消和字形。未完成 T12 打印/断网/P95 与 T13 图形化安装验收。
- 已于无 `acad` 进程时执行 `scripts/build.ps1 -Target Package`：0 警告、双框架各 144/144、bundle 清单 22 文件通过。候选 ZIP：`artifacts/packages/20260925-011426-655/JUSTIFIED_specification-reflow-for-AutoCAD-0.1.0-candidate-314e6c9.zip`，SHA256 `EECAB08E14EA6DEB03E7A97E8B5E272C985C2FF64570DDBBF2F5EE76A6D54A18`。BUILD.json 的 `workingTreeDirty=true` 来自原有测试夹具换行和未入库业务资料，已明确保留；源码提交 `314e6c9`。
- 当前 Windows 用户本机插件目录已替换为新候选：`%APPDATA%/Autodesk/ApplicationPlugins/JUSTIFIED_specification-reflow-for-AutoCAD.bundle`；旧版原位备份为 `JUSTIFIED_specification-reflow-for-AutoCAD.bundle.backup-20260925-0114`。已比对安装后的 PluginHost DLL 与候选相同，SHA256 `1A1723C995E4CDCE2BFB9B5C684511D52062EB963636AA3486296DAE875DCE12`。这只证明文件部署，不等于 CAD 宿主已运行或图形化安装器验收。

## 2026-09-25：0925 宿主截图反馈后的视觉修复

- 用户回传 `测试结果/0925/页面很差.png` 与命令记录，指出新版窗口比例失控、控件遮挡、底部大量空白和未加入其 logo。对比 V2 意向图：用户截图窗口约 1107×1034，内容只占上部；原实现将固定坐标、`AutoScaleMode.Font` 与最小窗体尺寸并用，导致窗体与内容缩放不一致。上一版仅放临时菱形，没有利用已提供的 logo 草图，这不符合用户同意按 V2 继续修改的目标。
- 从用户 `测试结果/0924(1)/logo设计图_名字俩字还应该在想想.png` 提炼三段式黑色图形，去掉待定文字，保存为 PluginHost 嵌入资源 `Assets/note-logo.png`。这是从草图提炼的候选图标；后续若提供 SVG/AI/透明 PNG 原稿，可原位替换。未把未定的中文名称写入界面。
- 选择窗调整为 1000×766 的无标题栏布局，禁用 WinForms 自动字体缩放，采用像素字体与固定布局，按 V2 稿重排页眉、Word、四张图幅卡、模板、单位和页脚。模板摘要显示“结构说明 · 版本（来源）”；仅缺模板时显示选择目录入口。离线 WinForms 预览 `artifacts/t14-ui-preview.png` 已生成并目视核对，画面没有 0925 截图里的大空白或遮挡；真实 CAD 宿主的缩放/焦点仍需复测。
- 同一份 0925 命令记录可确认旧候选在该家中电脑 `DN_DIAG_OK`；A1 2 页、227 对象、0 警告，点位后 `anchor_to_finish=1377.769 ms`，其中 `cad_measure=625.6985 ms`、`measure_calls=8005`。这是一笔业务样本的实际计时，不代表 P95。用户确认先前 `m³` 和负指数已改善，但指出 `N/mm²` 中的平方显示为问号，修复见 T12 日志。
- 本轮代码提交 `c3921cd`；`scripts/build.ps1 -Target Package` 0 警告、net8/net48 各 144/144、bundle 22 文件核验通过。候选 ZIP 为 `artifacts/packages/20260925-145525-086/JUSTIFIED_specification-reflow-for-AutoCAD-0.1.0-candidate-c3921cd.zip`，SHA256 `A9EC1B5436EEBF6351263B29F001BBBC49BFFA203F778E93A03689057443DDC9`。离线预览已保存为 `docs/assets/cad-note-picker-v3-offline-preview.png`，不是 CAD 宿主截图。
- 构建时本机 `acad.exe` 仍运行（PID 2064）；为了避免替换正在加载的 DLL，新候选尚未覆盖 ApplicationPlugins。已异步请用户保存并退出 CAD；待其确认关闭后再部署并复测。当前安装的仍是上一候选 `314e6c9`，不能把本轮修复说成已经在 CAD 生效。

## 2026-09-25：管理员维护模板，设计人员只选三项

- 用户明确确认：日常只选择 Word DOCX、图幅和单位比例；模板版本由管理员在安装包后续维护，建筑、结构和工艺当前共用相同图幅模板。表格等能力列为后续范围，不把它当作本轮 V1 窗口修复。该决定写入 PRD 3.3 与 ADR-012；不修改共享协议、依赖或技术栈。
- `NotePickerForm` 删除模板版本控件，压缩为 1000×690。对当前所选图幅，只有 **恰好一份** production+calibrated 模板才启用点位按钮；多份时显示管理员冲突提示，缺模板时阻断，避免按目录顺序或图内上次版本偷偷挑选。`NoteSettings` 仍在每次生成后记下实际 ID/版本，便于审计和旧图兼容。
- WinForms 离线预览已更新并保存 `docs/assets/cad-note-picker-v4-three-input-preview.png`。`scripts/build.ps1 -Target All` 在删除版本栏后通过：0 警告、net8.0/net48 各 144/144。真实 CAD 中的三项窗口与 `N/mm²` 字形待新包部署后用户复测。
- 代码提交 `92ead49` 后 `scripts/build.ps1 -Target Package` 再次通过：0 警告、net8.0/net48 各 144/144，bundle 22 文件校验通过。候选 ZIP `artifacts/packages/20260925-154918-026/JUSTIFIED_specification-reflow-for-AutoCAD-0.1.0-candidate-92ead49.zip`，SHA256 `19DF8E2DD96A0C09234F58FCFE20D4451C87688812970987B776983531050F7D`。BUILD.json 保留 `workingTreeDirty=true`（原有测试夹具换行及本地业务资料），`productionReady=false`。
- 部署前确认无 `acad` 进程。已将上次候选备份为 `%APPDATA%/Autodesk/ApplicationPlugins/JUSTIFIED_specification-reflow-for-AutoCAD.bundle.backup-20260925-1549`，再复制新 bundle 至同目录标准名称；安装后 PluginHost DLL 与候选 SHA256 相同：`1508B1C0CFCF88002F3DB734DE45011F038AF0E44C208C3F9A9CB7387963398B`。这是本机文件部署，不等于真实 CAD 界面、字体或图面已经通过。用户下一步重启 AutoCAD 2021，在空白测试图核对三项窗口、logo、A1/A2/A3、`N/mm²` 和取消行为；异常反馈截图与 F2 命令行。
