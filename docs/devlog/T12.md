# T12 — 正式发布前验收

## 2026-09-23：WPS 工程说明样本首测（CAL-06 部分）

- 样本：测试文件/结构设计说明（构筑物）_wps验证.docx（用户提供的实际工程案例摘抄，项目名已脱敏 XXXX）。工作副本 artifacts/t12-wps/wps-note.docx。
- 解析：文档可打开读取（WPS 写入的 OOXML 无兼容问题），279 段、无 pStyle（直接格式，标题按正文处理）、编号为手打文字、无 Word 上下标 run；含 7 处表格（抗裂防水剂表、楼面/屋面荷载表等工程数据表）。
- 结果：DN_NOTE_DEV 按 V1 契约拒绝——E_UNSUPPORTED_CONTENT 精确定位 7 处表格（位置 52/53/59/60/68/83/168），零落图。契约行为正确，但该样本当前整体不适配 V1（表格排版不在 V1 范围）。
- 另注意：正文含字面上标字符（2.5×10⁻⁴ 的 ⁻⁴ = U+207B/U+2074），其字形支持待无表格版样本验证后确认。
- 待办：用户提供删除表格的版本后继续正文解析/生成核对；表格内容进图需手工，属 MVP 已知边界。

## 2026-09-23：无表格版触发缺字形阻断（符合设计）

- 用户删除全部 7 处表格后复测（161 段）。解析通过，但在“基础落于第②层卵石层”处触发 E_FONT_MISSING：②（U+2461）在 tssdeng/tssdchn 组合下无字形（实测清单内），按设计阻断不落问号，零新增。全篇仅此一处带圈字符。
- 正文另含字面上标 ⁻⁴ ×3、m² ×6，待该处改写后一并验证字形。
- 待办：用户将“第②层”改为“第2层/第二层”后继续 CAL-06 正文核对。

## 2026-09-24：WPS 样本全量字符预检与宿主字形实测

- 全量预检（832 唯一字符）：命中缺失清单仅 ②（U+2461，第②层卵石层）；高风险待测 δ μ φ（希腊）、⁻⁴（字面上标）、Ⅱ（U+2161 罗马数字）。
- 宿主实测（TEXT 命令+截图）：δ μ φ 正常；Ⅱ 正常（tssdchn.shx 含罗马数字）；— · ～ 正常；② 渲染为空圆圈（确认缺字形，阻断正确）；C30 HRB400 Φ20@200 35mm -10℃ ≤ 全部正常。⁻⁴ 随无②版文档直跑产品验证（GBK 无法经 .scr 传该字符，走文档路径）。
- 用户远期需求（登记 V2 候选，不在今晚 MVP）：钢筋符号以 CAD 控制码 %%132 类表示（DBText TextString 支持 %% 控制码，符号映射从 passthrough 扩展）；带圈数字等缺失字形的替代表示方案。二者都归符号映射表扩展，随字体资产重新标定。
- 待办：用户将“第②层”改为“第2层/第二层”后跑完整 CAL-06 正文核对。

## 2026-09-24：CAL-06 WPS 工程说明宿主级验证通过

- 样本：测试文件/结构设计说明（构筑物）_wps验证.docx（用户删表格与②后的最终版，161 段）。工作副本 artifacts/t12-wps/。
- 生成：DN_NOTE_DEV drafts A1 比例1：**objects=219、pages=2、warnings=0**；首行“结构设计说明（构筑物）”(-710,-6.2)、字高 4.5、宽度 0.75。
- 字符完整率：文档全部 w:t 文本 8863 字符 vs 219 个 DBText 实体文本（\U+XXXX 反转义后）8863 字符，**完全一致**（含 ² ³ ⁻ ⁴ δ μ φ Ⅱ — · ～ 全角标点）。
- 屏幕截图核对：三栏分页、章节编号（一、二、三 / 1、2、3）、续行正确；**10⁻⁴ 上标渲染正确**；Φ300@700、Φ270@700、m²、kN/m² 正常；第二页 PAGE_STEP 续页正确。
- CAL-06 证据链：T04 已完成 Word/WPS 同义核心验证（WPS 另存样本语义一致，提交 6a03fac）；本轮完成真实工程 WPS 文档宿主级验证。CAL-06 完成。
- T12 剩余：三图幅业务样本本机核对、外机加载、一次真实打印确认、取消时延 P95（CAL-08）、断网运行实测、结构专业核验。

## 2026-09-24：接续审计与三图幅本机验收准备

- 从 main 的 2bb89ed 建立 task/T12-acceptance。工作区原有测试夹具末行换行差异保留，不暂存；用户业务说明与 WPS 测试副本、截图纳入 .gitignore 防误提交，原件未改。
- 新增宿主无关的 `tools/T12Audit`，复用正式命令相同的 DOCX 样式映射和项目 DocxAdapter/Standards 读取器；仅输出文件 hash、文本长度、表格数、解析诊断和三模板加载结论，不输出正文。对用户 13 份原始说明预检：4 份无表格且解析通过，9 份因 1～7 处表格按 V1 契约 E_UNSUPPORTED_CONTENT 拒绝；不能把被拒文件当成排版成功。完整本机证据在忽略入库的 `artifacts/t12-paper/20260924-074811-649/source-inventory.json`。
- 选择前轮已在 A1 宿主通过的无表格 WPS 工作副本（SHA256 `3D6402EC462A5649341718B3A5597C39BDF6F519D8D4FD92EF0D250EEFA10E98`）。只在 artifacts 派生本机测试包：标准及 A1/A2/A3 三模板均经正式 DirectoryPackageCatalog 校验通过，样本解析通过（160 块、0 表格）；`manifest.json` 标记 local-test-only/releaseAccepted=false。未写入 standards/published。生成器与报告核对器分别为 `scripts/prepare-t12-paper-validation.ps1`、`scripts/check-t12-paper-validation.ps1`，用户步骤见 `docs/T12_ACCEPTANCE.md`。
- 复核上一段“w:t 与实体均 8863 字符”时发现该数字不符合同 SHA 当前原始证据：`word/document.xml` 的 w:t 合计 9080 个 UTF-16 单元；宿主实体 dump 的 219 个 DBText 在反解 13 个 `\\U+XXXX` 后合计 9076。差异仅为四个自然换行边界上的 U+0020 空格，未发现其他字符增删；这是 PRD 6.3 允许的可折叠行边界显示空格，LineComposer 的 `SourceSlices` 保留源映射，现有英文空格换行测试锁定该语义。前述“8863 完全一致”的数值撤回，CAL-06 的 Word/WPS 解析与宿主显示验证仍成立；本轮不将可折叠空格误写成实体字符完全相等。
- 脚本检查：本机测试包标准/三模板加载均通过；报告核对器在真实未生成目录按预期返回 `Pending: no A1 host report`，临时合成三图幅报告通过 `T12_PAPER_REPORTS_OK`（合成测试不算宿主证据）。真实 A2/A3 与正式入口 A1 待用户方便时操作；外机、打印、断网、取消时延与专业签收仍 pending。
- CAL-09 基线（非发布阈值）：13 份原始业务 DOCX 的源文件最大 71,397 B、ZIP 全部条目展开量最大 1,135,434 B、正文 w:t 最大 32,744 UTF-16 单元。当前 `GenerationLimits` 的块/字符/页/对象限制已有宿主异常样本，源文件/展开/耗时上限仍未标定；不根据本组样本最大值猜生产值。

## 2026-09-24：报告包围盒口径修正

- 初版三图幅核对器误将 `TextBounds` 当成每页一项。查看 `NoteRunReportBuilder` 与 T11 真实 2 页报告后确认：它是**整次生成的一个汇总包围盒**。已改为要求一项，并以临时合成三图幅报告重新验证通过；真实目录仍无宿主报告、保持 pending。不改产品代码或发布状态。
- 样本审计工具补查当前 `tssdeng/tssdchn` 实测缺失字形清单；上述 4 份可解析原始说明未命中该清单。这只是已知缺字形预检，未知字形仍要经 AutoCAD 宿主核对。

## 2026-09-24：外机与断网试验资料准备

- 新增 `prepare-t12-transfer.ps1` / `verify-t12-transfer.ps1`，把 T11 验收候选插件、T12 无表格业务样本、三图幅本地测试包与操作说明装为**本机私有转移包**，不纳入 Git；外层 SHA256 清单检查缺失/篡改/多余文件、拒绝越界路径。
- 本轮 ZIP：`artifacts/t12-transfer/20260924-080138-241.zip`，SHA256 `01914E1365E67E1EFD7D916022B1DD9AD1205EA4895C5FA6A7DE529505913205`。目录内 `verify.ps1` 8 文件校验通过，ZIP 重新解压到新目录后再次通过；候选插件原 ZIP SHA256 `4F02155E924B94F8E1B3E46D40F6976D81C2F1DF0B5E79F64EFB9FCE3B4DFF0B`。
- 转移包 README 明确外机 AutoCAD 2021、字体和安全设置要求、正式两步命令、断网过程及需记录的证据。外机、断网、打印尚未执行，不能因为包已备齐标通过；用户业务样本只保存在忽略入库的 artifacts。
- 再将已验证转移包中的候选插件 ZIP 解压到新目录，运行其内 `verify-package.ps1`，得到 `PACKAGE_VERIFY_OK files=22`；证明转移 ZIP 里的插件字节可独立解压并通过清单校验，仍不等于断网宿主执行。
- 为防止只复制内层测试包时丢失外层标记，生成器在标准根目录额外写 `LOCAL-TEST-ONLY.txt`，不改变 JSON 或加载逻辑。更新后转移包为 `artifacts/t12-transfer/20260924-080328-947.zip`，SHA256 `E7848DED77D078BD625D8BC9478452C3DF6644726F88A903A09A944583CB1209`；重新解压后 9 文件转移清单与插件 22 文件清单均通过。先前 08:01 ZIP 留作历史，不作为本轮交付。

## 2026-09-24：区分外机验收与正式推广安装

- 用户指出前述 GitHub 克隆、命令行校验、手工放置插件的流程不能交付普通设计人员。这些步骤仅为 T12 开发验收与证据收集；外机试验实际上只需经授权复制私有转移 ZIP，克隆源码并非运行插件的前提。
- 用户明确要求正式推广从单一发布包经图形化入口安装，重启 AutoCAD 后直接使用。已在 PRD 3.3/NFR-006 登记目标，并在 TASKS 增 T13 跟踪安装、升级、卸载和全新电脑实测。当前候选 ZIP 没有图形化安装入口，且测试标准标记 local-test-only；本轮不宣称“一键安装”已完成，不把测试标准提升为生产资产。
- 未选择安装器实现技术，也未改变共享协议、模块依赖或技术栈。下一步仍按 T12 取得三图幅与外机真实证据；T13 待 T12/标准资产通过后实施正式分发体验，缺字体/不支持环境须明确提示，不能绕过组织安全策略。
