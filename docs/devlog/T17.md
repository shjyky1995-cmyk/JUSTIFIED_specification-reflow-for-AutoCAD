# T17 设计说明新 PRD 与实施计划

## 2026-09-27

- 原因：用户提供今天的产品讨论与 Figma Make 初稿，要求先完成 MD 版 PRD 和阶段计划，再决定下一步开发；新三步业务流程取代 D0 固定栏目作为桌面主流程。
- 改动：新增 `docs/PRD.md`、`docs/IMPLEMENTATION_PLAN.md`，标注 D0 历史状态，更新任务与接续。T16 已实现工作在独立分支提交 433ec3a 并推送，未合入 main；原测试夹具本地换行差异未暂存。
- 核对：通过 Codex 读取指定 ChatGPT 对话；Figma 连接返回 Make 文件目录链接但未返回可直接检查的当前画面，浏览器会话读取失败，因此 UI 仅归纳已确认讨论和设计方向，当前视觉稿需后续直接核对。仅文档变更，执行链接/状态/差异检查，不把桌面功能当已验收。
- 下一步：用户评审两项待定业务规则及 UI 导航基线；确认后按 P1→P4 推进 T16 分支重构、实测和验收。

## 2026-09-27：按用户补充决定修订

- 用户决定：六专业入口都应可选并走完章节编辑、预览和生成；非结构不展示结构参数；专业正文后期由用户统一补充。开发不设日历时间，按大阶段连续做完再验收；当前只做前期工作、不写业务代码。
- Figma `whoami` 确认账号连接；`get_design_context` 识别指定 Make 项目但仅给源码资源链接，当前环境无法读取资源正文；浏览器 CUA 三次返回 `nodeRepl.fetch request failed`。不能写成已看过当前视觉稿。
- 数据调研：agent-reach 的 GitHub 代码搜索未核实可用于全国工程生产的完整、许可明确的烈度数据包。国家标准平台显示 GB 18306-2015 现行，2026 年公告说明有修改单；地方地震部门指向官方区划图查询服务。新增来源记录，建议先保留「待核定」，后续检查官方接口/许可或导入经核定的数据。
- PRD 和计划改为先通用编制框架、后专业内容、最后成品交付的三个大阶段，内容缺失不阻断框架阶段，也不生成虚假的规范结论。
