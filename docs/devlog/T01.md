# T01 — 规范与 Git 基线
## 2026-09-21
- 输入：CAD_DesignNote_PRD_V1.0.md、用户确认的技术栈与协作计划。
- 输出：精简 AGENTS.md、架构、任务清单、CAL 台账、开发入口和日志。
- 范围：文档、Git 忽略与文本格式规则，不修改 PRD、不开发业务功能。
- 原因：保证新对话与不同 Agent 能按任务接续并保留提交历史。
- 验证：确认目录原先只有 PRD，尚未初始化 Git；本机可用 SDK 8.0.425。
- 环境发现：AutoCAD 2021 安装在 E 盘，托管 DLL 与 accoreconsole 存在，版本 24.0.47.0。
- 提交关联：git log --all --oneline --grep='(T01)'。
- 验证结果：AGENTS.md 24 行；入口文档与任务日志已建立；git diff --check 通过。
- Git：初始化任务分支，使用仅限本仓库的 Codex Agent <codex-agent@localhost> 自动化身份。
- 状态：完成；本次提交建立初始基线，接续 T02。GitHub 地址尚未提供，未远程备份。
