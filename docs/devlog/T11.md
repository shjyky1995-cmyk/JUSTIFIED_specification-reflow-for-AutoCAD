# T11 — 限额、运行报告与部署

## 2026-09-22：领取

- 输入：T10 的正式命令入口。REQ-OPS-001，AC-13～14（限额、运行报告、离线包）。
- 输出：单次生成限额、本地运行报告、离线打包与部署文档所需的剩余项登记。
- 范围：Contracts 限额参数、Application 限额与报告组装、Standards 编解码与模板枚举、AutoCadAdapter 图内设置存储、PluginHost 正式命令。不改共享协议语义，不改变依赖方向。

## 2026-09-22：限额与报告

- 限额：`GenerationLimits`（MaxPages/MaxTexts/MaxBlocks/MaxCharacters）在解析后、布局后、放置后三个检查点执行，超出以 E_RESOURCE_LIMIT 拒绝并清空 Texts，不截断、不落图。限额为 null 时保持旧行为（兼容 DN_NOTE_DEV）。
- 运行报告：`NoteRunReport` 记录开始/结束时间、耗时、图纸、文档、标准与模板 id/version、图幅、单位比例、成功与是否提交、页数、对象数、告警/错误码、文字包围盒。宿主在报告目录非空时写 JSON；写失败只告警不阻断生成。
- 检查：核心测试双框架 131/131（新增 14 个：设置往返/损坏/Problems、四个限额触发点、限额内生成、无限额兼容、报告字段、模板枚举/取消）。宿主编译 0 警告。
- 未完成：宿主实测限额与报告文件、性能（冷/热生成耗时）、断网离线包验证、部署文档。跟随 T10 的宿主验证一并进行。

## 2026-09-22 17:5x：宿主核对伴随 T10 完成

- 限额与运行报告已在真实宿主随正式命令执行：三次生成均写报告 JSON（含耗时 790ms、包围盒、告警空）；限额路径由核心测试覆盖，宿主未构造超限样本。
- 未完成：冷/热性能对比、断网离线包、部署文档。DLL SHA256 前 8 位 cbf99922（含 eKeyNotFound 修复），交付副本 artifacts/cad-retry-note3/plugin/。

## 2026-09-23：阶段3接续与可复现报告

- 用户要求代验证后开始下一步。基线 cc219b7（保留独立核对记录），从 main 创建 task/T11-offline-delivery，再 fast-forward 接入 task/T09-dbtext，main 未变。远端阶段代码备份核对通过；基线核心双框架各 140/140、插件编译零警告。
- Application 内部加入源读取计时包装，分别记录读取、解析、测量/排版与坐标放置；报告增加输入 SHA256、引擎及规则版本、视觉行数。共享 Contracts/Schema 与依赖方向未改。
- 正式宿主命令独立记录准备、等待用户、落图及单调总时长；明确首次命令执行并非宿主启动耗时。标准加载后失败/点选取消/警告拒绝也写报告，文件名增加唯一后缀防覆盖。
- 新增一万字符的重复生成、非结构 disciplineCode 几何一致性、输入失败资源释放与报告耗时隔离测试。首轮新样本漏设样式映射被正常拒绝，补齐夹具后双框架各 142/142、插件编译零警告。模拟测量结果不作为宿主性能签收。
- 接续：打包入口仍为 M0，继续完成候选离线包、完整性检查及部署说明；正式标准三图幅与外机/打印仍 pending。

## 2026-09-23：离线候选交付与宿主测试准备

- 打包从 M0 诊断更新为 validation-candidate：正式 DN_NOTE_SET/DN_NOTE 按命令加载；包内有运行依赖、Schema、版本/源码提交/工作区状态、安装回滚说明与 SHA256 清单。正式标准目录尚无发布 JSON，明确标记 productionReady=false，未混入本机验收资产。
- 新增 verify-package：检查完整性、缺失/多余文件、路径越界、运行依赖和命令注册，排除 SDK/字体。候选包共 22 个文件校验通过；新目录解压通过；篡改 BUILD.json 被拒绝，恢复后通过。Windows PowerShell 5.1 复测发现 ConvertFrom-Json 数组包装差异，修复后同样通过。
- 新增 summarize-performance：同输入/规则组至少 20 次暖运行，P95 按 nearest-rank 计算，用户等待不计入点选后耗时；拒绝混合输入或旧报告。合成报告核对 20 次暖运行、等待 5000ms 被扣除、点选后 P95=110ms，混合 hash 被拒绝；这些数值只是脚本测试，不是 CAD 性能证据。
- AutoCAD 2021 独立空白 Drawing1 已启动，准备 10000 字合成 DOCX（SHA256=1298CEDEEE01DE9F6358F9A22648E0A7EA7BF4FC5CD962AC864DA6B3F5FF553A）、首次+20次暖运行脚本 artifacts/t11-host/benchmark.scr，每次生成后 U。
- 真正阻塞：NETLOAD 停在“安全性—未签名的可执行文件”，computer-use guidance 禁止 Agent 操作安全许可请求，已请求用户亲自点击“加载一次”。未修改 SECURELOAD/信任路径；脚本尚无报告，宿主性能不能标通过。弹窗目标为 artifacts/packages/20260923-175057-362 下候选 DLL。
- 不依赖宿主的开发和检查已完成；T11 继续进行中，T12 尚未启动。剩余：真实性能/离线/取消及资源标定、正式三图幅标准与外机/打印/专业复核。
- 工作区另出现 test-note-standard.json 末行换行差异（文件时间 17:43:27），不属本次功能修改，保留且不暂存。

## 2026-09-23：本轮备份与候选包复核

- f7a1576（报告/复现）和 e03d01d（候选包/校验/性能汇总）已推送 origin/task/T11-offline-delivery，ls-remote 核对 e03d01d 一致；远端 main=c8e75fd 未变，未打标签。
- 最终候选包 artifacts/packages/20260923-175653-822/JUSTIFIED_specification-reflow-for-AutoCAD-0.1.0-candidate-e03d01d.zip；SHA256=911ADE7635B52DFF1A6588BEB1C386652FB249739CD7D5167B1940D0045529FF。完整重打包双框架各 142/142、零警告；新目录解压后使用包内 verify-package.ps1 在 Windows PowerShell 5.1 校验 22 文件通过。
- BUILD.json 保留 workingTreeDirty=true（未暂存夹具末行换行差异），productionReady=false，不伪称干净正式发布。当前 CAD 弹窗仍引用先前 17:50 候选包；其功能代码同为 f7a1576，后续变化为打包/文档/汇总脚本。
- 本轮暂停点为用户处理“加载一次”；未取得本轮宿主报告，T11 不标完成。

## 2026-09-23：用户加载后完成宿主验证及取消补齐

- 用户完成“加载一次”，独立 Drawing1 执行21次万字生成：20次热运行点选后P95=373ms、准备P95=1.0939ms、0失败；每次1页200对象。首次命令805ms，不声称是宿主冷启动。证据详见 docs/T11_HOST_VALIDATION.md。
- 15000字两次均2页300对象，正文/位置/样式/包围盒快照hash完全一致，撤销后零TEXT；501段和201000字超限、损坏DOCX均失败零残留；点选Esc取消报告正常。
- 发现处理中未传取消令牌、预检失败缺报告。现加入仅监测当前CAD前台Esc的25ms轮询，后台不调用CAD API；令牌贯通解析/排版/事务写入，释放与回调互斥。ZIP条目检查响应取消，页数安全限制提前使用既有200页上限，不改变共享契约或限额值。
- 文档不存在/标准加载失败/版本不符/字体缺失均记录错误；报告存在错误时不再声称成功。读取中取消测试确认只读一次、返回CANCELLED、无文本且资源释放。
- 双框架各143/143，插件编译零警告。新代码宿主按键取消与预检报告待新实例加载验证，不能用旧二进制成绩代替。T11进行中，不合main、不打标签。

## 2026-09-23：用户调整宿主操作分工

- 用户要求先了解流程与后续计划，并明确为了节省额度，默认由用户方便时操作AutoCAD；不方便时再告知Agent代操作。已写入AGENTS长期规则。
- 上轮8f0225b候选包22文件校验通过；随后Computer Use报告实体Esc停止，未启动新版宿主验证。修正NEXT_AGENT中旧的等待加载状态，保留新版未验证和未推送事实。
- 本轮只改协作/接续文档，检查差异后提交，不启动Computer Use，不重复运行代码测试；main和无关夹具不改。
