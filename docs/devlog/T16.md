# T16 桌面 DOCX 编制第一阶段日志

## 2026-09-26：实现中的 checkpoint

- 用户确认桌面与 CAD 独立，桌面生成 DOCX，Word/WPS 修改，CAD 中手动重新选择文件；桌面界面要求与 CAD 白底、蓝灰、Noto Sans SC 视觉语言匹配，简洁且有质感。
- 建立 Electron + React/TypeScript 桌面壳、固定栏目填写界面和 DOCX 检查入口；界面仅调用预加载桥白名单，Electron 主进程只启动本机 .NET 工作进程，不连接 CAD。
- 建立 .NET 10 DOCX 工作进程：固定栏目写出 Open XML，立即用现有 `DocxDocumentParser` 读回；已有 DOCX 不覆盖；检查命令只读。中文 stdin 在 `Console.In` 读入时出现字节损坏，已改为从标准输入读取原始字节后严格 UTF-8 解码。
- 本机 .NET 10.0.401 SDK 安装在忽略目录 `artifacts/dotnet10/`；ZIP 经 Microsoft 官方 SHA512 校验。`System.IO.Packaging` 8.0.0 在 .NET 10 构建时触发已知漏洞警告，工作进程显式使用 10.0.12；未改变既有 CAD 插件依赖。
- 已核验：工作进程 Debug 构建零警告；`node scripts/test-desktop-worker.mjs` 通过生成、重新检查、拒绝覆盖和空内容阻断；`npm run build` 成功。Electron 可执行文件已装，但实际窗口视觉和端到端桌面按钮尚未完成检查，不标记 D1 验收通过。
- 用户要求尽快推送总体计划并讨论后续时间安排与最终产品目标。本分支暂存当前可复现代码，等待讨论后继续 D1，勿把 checkpoint 合入 main 或发布安装包。
