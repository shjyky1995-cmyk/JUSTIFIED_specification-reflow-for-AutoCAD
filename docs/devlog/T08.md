# T08 — 固定行槽与续页

## 2026-09-22：领取

- 输入：T07 提交 `29b9e39`。换行已经按当前栏宽逐行测量，本任务锁住分栏和续页。PRD 7.1、7.2，REQ-LAYOUT-002，AC-08、AC-09。
- 输出：核心测试。没有改 Contracts。生产草案仍是 pending / uncalibrated。
- 分支：task/T08-column-flow，继承 T07 和未验收的 T06，不合入 main。

## 2026-09-22：行槽

- 3 栏×2 槽：6 行是 1 页，7 行是 2 页，第 7 行在第二页首栏首槽。刚好写满没有空尾页。
- 源空行和连续硬换行占槽，不生成空 DBText。标题在配置的前后槽为 0 时不额外占行；配置改成 1 才插入空槽。
- 窄栏换到宽栏时，未消费文字按新栏宽重排，编号不重复。一段文字可以跨栏、跨页，行不丢、不重。
- A1/A2/A3 草案几何在内存中标成可测：4.5 mm、宽度 0.75、行距 7.2 mm、零缩进。A1/A2 三栏，A3 两栏。每套都做了满页、多一槽、空行和跨栏。文件本身没有改。
- 页数保护上限会拒绝再开一页，但刚好写满的一页仍然成功。字形高度超过行距时失败，不挤出额外行。
- 检查：`scripts/build.ps1 -Target Core`，0 警告 / 0 错误。net8.0 与 net48 各 110/110（T07 的 100 + 新 10）。测试类 `ColumnFlowTests`。
- 没有证明：真实字体下的行宽、上下标、图面和打印。没有 DBText 落图。T06 仍待验收。
- 状态：待验收。阶段 1 到此交付。下一阶段是 T09+T10，用户回复“继续下一步”再开始。
