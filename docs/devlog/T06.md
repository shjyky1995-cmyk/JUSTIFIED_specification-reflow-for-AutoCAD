# T06 — 标准校验与测量原型
## 2026-09-22：领取
- 输入：T05 已合入 main（c8e75fd）。PRD 5.3、REQ-STD-001、REQ-TEXT-001、AC-05、AC-06。CAL-02～CAL-07 仍是 pending。
- 输出：院标和模板的发布校验；宿主测量原型。正式目录不放入测试数值。
- 范围：Standards、AutoCadAdapter、测试夹具、`standards/published`。控制台入口放在已有的 PluginHost 命令和 `scripts/test-cad.ps1`，因为 NETLOAD 只发现插件宿主里的命令；测量实现仍在 AutoCadAdapter。没有改端口签名、项目引用方向或技术栈。读取 JSON 使用仓库里已有的 Newtonsoft.Json 13.0.3。
- 边界：StyleDefinition 没有单独字高，V1 只有院标上的一个 textHeight。LayoutTemplate 没有 resolvedRowPitch 字段，快照只在 JSON 里核对。standardBlock 没有图框块引用，不能当成已标定发布。

## 2026-09-22：缺标定阻断
- 改动：`PackageValidator` 拒绝空字高、pending 版本、未标定状态、空栏、非正行数、空 pageStep、相邻页重叠、标题或正文的额外槽、缺字体身份。`DirectoryPackageCatalog` 在正式模式下拒绝 classification=test-fixture。同名版本化样式若与院标不同，返回 E_STYLE_CONFLICT，不改旧样式。
- 测试夹具：`tests/.../Fixtures/test-note-standard.json` 与 `test-note-a1.json`，classification 为 test-fixture，standardId 为 test-note。其中的 2.5、0.8、4 只用于测试，不是院标。
- 正式目录 `standards/published` 只有说明，没有任何 json。
- 检查：`scripts/build.ps1 -Target All` 为 0 警告 / 0 错误。net8.0 与 net48 各 80/80。新增 5 个包校验测试直接调用 `DirectoryPackageCatalog` 和 `PackageValidator`。

## 2026-09-22：控制台测量未执行
- 改动：`HostTextMeasureService` 用 DBText 的 GeometricExtents 取 advance 和字形边界，事务不提交。缺字体返回 E_FONT_MISSING。上下标缩放尚未标定，直接阻断，不套用比例。`DN_MEASURE` 使用命令内的测试字高 2.5、宽度系数 0.8 和 txt.shx，会在输出里标明 test-command。
- 控制台：AutoCAD 2021 accoreconsole R.47.0.0。默认配置 SECURELOAD=1，TRUSTEDPATHS 为空。NETLOAD `PluginHost.dll` 的结果是「无法加载程序集」，随后 DN_MEASURE 为未知命令。没有修改 SECURELOAD 或 TRUSTEDPATHS。因此没有 advance、字形边界或 DBText 的实测输出。
- 没有证明，也不记为通过：AC-05 的三套正式模板合法；AC-06 的屏幕与打印字形、测量和实体边界误差。测试夹具通过校验不等于院标已发布。
- 状态：阻塞。本分支不合入 main。accoreconsole 因路径未受信任未能加载；改由图形界面 NETLOAD。

## 2026-09-22：图形界面 DN_MEASURE
- 操作：用户退出已加载旧 DLL 的 AutoCAD 后，重新 NETLOAD `src/Justified.SpecificationReflow.AutoCAD.PluginHost/bin/Release/net48/Justified.SpecificationReflow.AutoCAD.PluginHost.dll`，执行 `DN_MEASURE`。没有修改 SECURELOAD 或 TRUSTEDPATHS。
- 命令行原文：
  - DN_MEASURE_STYLE test-command textHeight=2.5 widthFactor=0.8 obliqueAngle=0
  - DN_MEASURE_FONT txt.shx
  - DN_MEASURE_TEXT W20
  - DN_MEASURE_DBTEXT DBText
  - DN_MEASURE_ADVANCE 5.6666666666666661
  - DN_MEASURE_BOUNDS 0,0,5.6666666666666661,2.5
  - DN_MEASURE_MISSING_FONT E_FONT_MISSING 找不到字体 missing-test-font.shx。不会用其他字体代替。
  - DN_MEASURE_SUPERSCRIPT E_TEMPLATE_INVALID 上下标缩放尚未标定，不能测量，也不会把上下标改成普通文字。
  - DN_MEASURE_ENTITIES before=21758 after=21758
  - DN_MEASURE_OK
- 含义：advance 与字形边界来自 DBText 的 GeometricExtents。文本是 W20，测试字高 2.5；若按 3×2.5 估算，宽度会是 7.5，与 5.6666666666666661 不同。缺字体被阻断，未标定的上下标缩放被阻断。测量前后实体数同为 21758，临时文字没有留下。
- 这次使用的是命令内测试参数和 AutoCAD 自带 txt.shx，不是已发布院标，也不是打印核验。
- 状态：待验收。本分支仍不合入 main。AC-05 的正式 A1/A2/A3 和 AC-06 的屏幕、打印对照还没有证据。
- 继续之前需要人提供：CAL-02～CAL-07 的生产字体、字高、栏距、A1/A2/A3 和打印核验。在此之前不开始 T07。

## 2026-09-22：读取用户提供的 A1 图纸，补充现状标定
- 任务仍为 T06；输入为用户授权的测试文件/新块.dwg，输出为 docs/calibration/T06-A1-新块-实测.md 和仅本地的 local/calibration 原始证据。修改范围仅文档/日志，不改共享协议、生产配置或代码。关联 CAL-01～07、STD-001/TEXT-001、AC-05/06。
- 通过现有 AutoCAD COM 只读属性与几何，通过内置 findfile 查询字体。无插件加载、无安全设置变更、无保存源图。实体数前后 680，DBMOD 前后 0，源文件 SHA256 前后一致；SECURELOAD=1、TRUSTEDPATHS 为空，前后不变。
- 确认 A1 横式三栏框（第三栏空），每栏宽 23000、高 51191、栏间 1000；现有行格 71、间距 721。正文常见字高 450、TSSD、宽 0.75、基线距 720；存在混合字高、偏移与局部不等距，不能直接发布为统一标准。
- 单位无单位（INSUNITS=0），不是已确认毫米；图框标记 1:100，但保存打印设置为布满图纸。记录矛盾，不用整齐值覆盖实测。
- 检查：完整枚举模型空间，坐标差计算核对，行格去重/计数，字体查找成功，源文件与安全设置未变；git diff --check。未做屏幕字形/实际打印核验，没有将其标通过。25 条真实句子候选仅本地保留。
- 状态仍待验收，不合 main、不发布模板、不进入 T07；下一步确认单位/比例、统一行距/字高、首基线，补齐 A2/A3、续页、真实文档样式和打印字符证据。源 DWG、字体及真实正文未入库。

## 2026-09-22：按用户授权制定统一文字格式
- 输入：用户明确表示院内没有完整标准，允许项目自由制定格式并写入文件。依赖为已读字体身份和 PRD 的统一字高/无额外标题槽约束；任务仍为 T06（STD-001/TEXT-001、AC-03/05/06）。
- 范围：docs/TEXT_FORMAT_V1.md、AGENTS、PRD、架构/任务/标定台账、published 目录说明和本日志；仅规则文档，不改共享契约、代码、历史 DWG 或安全配置。
- 决定：tssdeng.shx+tssdchn.shx；标题/正文/编号均高 4.5 mm，宽度 0.75，倾角 0°，行距 7.2 mm。首行续行起点均 13 mm，编号起点 0、悬挂 13 mm；编号至正文至少 1 mm，超宽明确报告。新样式 JSR_NOTE_V1、图层 JSR_NOTE_TEXT。记录真实字体 hash。
- 原因：样图存在混合字号及 720/721 差异，用户要求主动统一，无须继续等待外部院标。明确区分自主制定参数与历史测量，既有字形/打印证据门槛保留。
- 检查：核对 PRD 同字高、标题无额外槽、毫米内部坐标和显式尺度原则；git diff --check。仅文档变更，不运行与此次变更无关的代码测试。
- 本小步完成；T06 整体仍待验收。下一步按新规则实施标准包、设计/验证模板几何并完成真实宿主/打印核验；不再以缺完整院标作为阻塞。此前日志的等待格式值要求由本次决策取代，历史记录保留。

## 2026-09-22：用户补充可调模板与内联编号
- 用户要求保留格式/模板调整入口，本次采用既有字体和分栏结构自行设计；随后明确编号在单行文字内部以空格与正文分离。此决定取代上节尚未提交的13 mm编号预留区方案。
- 最终规则：编号+两个U+0020空格+正文，首行续行均从栏左开始，indent/hangingIndent均0；普通文字首行一个DBText，必要上下标仍按原有分段契约。编号参与真实测量，不重复编号，不孤立编号，不改正文内部空格。
- 格式与模板分开版本化，沿用既有契约；样式命名采用现有standardId-version-role规则，不另创命名机制。A1测试明确采用三栏230 mm、栏距10 mm、高511.91 mm、首基线-6.2、行距7.2、每栏71行；续页向右861 mm，只排文字。以上为设计选择，非旧图完整实测复刻。
- 新增 docs/A1_TEST_TEMPLATE_V1.md、standards/drafts 的格式与模板草案及说明；保留classification=draft、uncalibrated、未验证字段pending/null。无生产发布，不改共享契约，不宣称已经实现可视化编辑器或编号渲染。
- 检查：两份JSON可解析；三栏尺寸、首末基线、页边界、页间距及标准引用一致；统一字号/标题无额外槽/零悬挂与最新规则一致；git diff --check。字体实际外包边界和打印仍未验证。
- 本次规则制定与配置草案完成。下一步实现内联编号排版并按本次A1规则开展真实字体及图面测试；模板可调，不再向用户索取完整院标才能继续。
