# T02 — 基础骨架与环境验证
## 2026-09-21：领取
- 输入：T01 文档基线、实际 AutoCAD 2021、SDK 8.0.425。
- 输出：8 个分层类库、NUnit 测试、构建/打包入口、最小诊断插件。
- 范围：src、tests、scripts、build、打包与依赖配置，相关文档。
- REQ/AC：DEP-001～008、OPS-001 基础；不宣称业务 AC 已通过。
- 决策：固定历史兼容版本，先验证再确认 M0 基线；不自动追逐最新版。
- Git 历史：git log --all --oneline --grep='(T02)'。
- 纠正 T01：首次未暂存 diff 检查未覆盖新增文件，暂存后发现空尾行警告；T02 清理并重新检查。
- 待执行：还原、双运行时测试、宿主编译、控制台真实加载、包检查。

## 2026-09-22：命名和远程
- 用户明确正式名称 JUSTIFIED_specification-reflow-for-AutoCAD；同步解决方案、安装包、文档；C# 使用合法命名空间前缀。
- 用户指定提交身份 Stephen Kim <shjyky1995@gmail.com>，仅更新仓库配置；保留 T01 自动化提交历史。
- 已配置用户提供的 origin；GitHub API 核实 isPrivate=true，初始远程无分支；本次完成验证后推送。

## 2026-09-22：构建与验收结果
- 工具：SDK 8.0.425 / MSBuild 17.11.48；Windows 10.0.26200 x64；Framework 注册版本 4.8.09221（Release 533509）。
- AutoCAD：2021 24.0.47.0，E:/Program Files/Autodesk/AutoCAD 2021；三份托管 DLL 仅引用、不入库、不入安装包。
- 实现：八个分层项目，无业务占位类/空服务；唯一命令 DN_DIAG 只加载并核对依赖版本，不写图纸。
- scripts/build.ps1 -Target Core：不查找 CAD；net8.0、net48 各 4/4 测试通过。
- scripts/build.ps1 -Target Package：锁文件还原、全方案编译 0 警告/0 错误，双运行时各 4/4 通过。
- 检查内容：Open XML Unicode DOCX 回读、JSON 扩展字段依赖行为、项目依赖方向、AGENTS 行数和入口链接。不是正式 DOCX 解析器或业务 Schema 测试。
- 限制：项目引用与基础源码检查只是首层架构守卫，未来业务实现仍需审查；当前无生产标准和模板。
- 包：artifacts/packages/20260922-000557-236/JUSTIFIED_specification-reflow-for-AutoCAD-0.1.0-m0.zip；含 11 个运行 DLL、许可证与逐文件 SHA256，不含 Autodesk SDK。
- 真实控制台两次 NETLOAD 均返回“无法加载程序集”，未执行诊断；未修改 SECURELOAD/TRUSTEDPATHS。原因尚未被完整证实，不能将其写成兼容性通过。
- 试装目录：当前用户 AppData/Roaming/Autodesk/ApplicationPlugins/JUSTIFIED_specification-reflow-for-AutoCAD.bundle；保留供用户正常图形界面 NETLOAD。系统 Program Files 写入被 Windows 拒绝，未成功创建系统安装目录。
- 当前试装包是 00:03 的第一份包；00:05 包增加严格版本断言。用户已打开 CAD，不能覆盖已加载 DLL；最终验收时退出 CAD、更新完整包后再验证，或使用新包路径正常 NETLOAD。
- 原始 CAD 输出在 artifacts/cad-smoke/20260922-000146-975 与 20260922-000324-150；原始 TRX 在 artifacts/test-results/。
- 用户已收到真实加载操作请求；尚无结果。T02 保持待验收，不合入 main，不打 M0 完成标签。
- 接续：读取用户返回的依赖版本/错误；确认最终包 DN_DIAG_OK 与依赖版本后完成 T02；合并 main、阶段标签与推送；再启动 T03。
- Git：main 已有 T01 规范和用户命名确认；T02 作为 checkpoint 推送任务分支以防丢失，未验收不冒充阶段发布。

## 2026-09-22：简化手工测试入口
- 用户找不到 AppData 内的试装文件，要求放在开发目录或同级目录。
- 新入口：仓库根目录「测试文件/开始测试.md」；「程序」内放最新严格版本断言诊断 DLL 及全部依赖，旁边放完整 ZIP。
- Package 构建自动同步此入口；二进制不入 Git，说明、构建规则和日志入库。
- 验证：从已校验的 00:05 安装包复制，逐 DLL 对比 SHA256；检查构建脚本语法与 Git diff。
- 未改变 CAD 安全设置；真实加载验收仍等待用户 DN_DIAG 输出。
