# T03 — 数据协议（模型、Active 契约、Schema、JSON 往返）
## 2026-09-22：领取
- 输入：PRD 第 5/9 节、AC-04 与 REQ-DATA-001；T02 已验收基线 main bf9ad78（标签 m0-foundation）。
- 输出：Contracts 数据模型与 Active 端口、schemas/ 三份 JSON Schema、DocumentCore 协议校验与序列化、AC-04 对应测试。
- 范围：Contracts、DocumentCore、schemas、tests 相关文件、docs/TASKS.md、docs/ARCHITECTURE.md 与本日志；不开发 DOCX 解析、排版算法、CAD 输出与 Future 端口。
- 边界决策（影响共享协议，先记录后实施）：
  - Contracts 保持零包依赖（现状无 PackageReference）；JSON 属性命名、枚举字符串转换与 Schema 校验全部落在 DocumentCore，避免变更模块依赖方向，也避免把 Newtonsoft 扩散到 Contracts。
  - 不新增 NuGet 依赖；JSON Schema 校验为自研 draft-07 子集：type/enum/const/properties/required/additionalProperties/items/minimum/maximum/exclusiveMinimum/minLength/pattern/minItems/if/then/else/not/$ref（本地与跨文件）。
  - 新增诊断码 E_SCHEMA_INVALID 用于一般协议违例（缺字段、类型错误、未知属性、非法数值等）；E_SCHEMA_VERSION 严格限于 PRD 第 10 节定义的“协议主版本或必需块不支持”。该新增码只扩展 Diagnostic.code 取值，不改变既有码语义。
  - 硬换行按 PRD 5.2 以独立 break run 表示：semantic=normal 且 text 恰为 "\n"；semantic 枚举保持 normal/superscript/subscript 三值。
  - 院标与 Layout Template 的 C# 类型随 Active 端口一并冻结（IStandardProvider/ILayoutTemplateProvider 返回类型），但其 JSON Schema 与发布校验随 T06 标定落地；PRD 5.3 示例“刻意不是可运行配置”，未标定前不冻结其 Schema 约束。
  - LayoutResult 与 RenderRequest 契约按 ARCHITECTURE.md“接口演进”在 T03 冻结，随 Schema 与往返测试一并交付。
- REQ/AC：REQ-DATA-001 / AC-04；不宣称其他 AC 通过。
- 实现步骤：①分支与日志 → ②Document 模型与诊断 → ③标准/模板/排版/渲染模型与几何 → ④Active 端口 → ⑤Schema 与 fixture → ⑥校验器 → ⑦序列化与协议门面 → ⑧AC-04 测试 → ⑨Core/All 双运行时验证 → ⑩文档、合并与推送。
- 验收测试：有效 fixture 往返语义不丢；未知扩展字段（extensions 命名空间 key）原样保留；未知主版本/未知块类型以 E_SCHEMA_VERSION 拒绝；缺字段/类型错误/未知属性/负索引/NaN/Infinity 以 E_SCHEMA_INVALID 拒绝；序列化确定且禁止 NaN/Infinity；documentHash 稳定。
- Git 历史：git log --all --oneline --grep='(T03)'。

## 2026-09-22：实现与验证（fdc8e55 → 5a98455）
- Contracts（零包依赖保持）：Document/SourceInfo/Block/TextRun/Numbering/SourceRef/TextRange 及枚举；Diagnostic/Severity/DiagnosticStage/DiagnosticCodes；院标（InstitutionStandard/FontProfile/StyleDefinition/LayerPolicy/StandardRef/TemplateRef）；模板（LayoutTemplate/ColumnGeometry/PageBounds/TemplateAnchor 及枚举）；排版与渲染（LayoutResult/Page/Column/RowSlot/VisualLine/RenderRun/ResolvedStyle/LayoutStatistics/RenderTransform/RenderRequest/RenderReport）；几何值类型 Point2/Bounds2（毫米、双精度）。fdc8e55、2cc0306。
- Active 端口（7ddaa8e）：IDocumentSource/IDocumentParser/IStandardProvider/ILayoutTemplateProvider/ITextMeasureService/ILayoutEngine/ICadRenderer；签名按 PRD 9.1 带 CancellationToken，结果类型携带 Diagnostics；Reserved/Future 端口未建。
- schemas（03b91d4）：document/layout-result/render-request 三份 draft-07 Schema（根目录 schemas/，作为 DocumentCore 嵌入资源，单一来源）；fixtures 10 个（1 有效 + 7 无效 JSON 文件 + 布局/渲染各 1 有效），无效项覆盖未知主版本、未知块类型、缺必需字段、未知属性、错误 semantic、负索引、spacer 缺 slotCount。
- DocumentCore：draft-07 子集校验器（6cbebe4，$ref 基准文档随跨文件引用切换；未知关键字忽略以前向兼容）；序列化设置与协议门面（e06a13e：camelCase 属性、字典键原样、null 省略、枚举 camelCase，PaperCode/AnchorKind 用显式名称转换器；Save 走完整序列化管线后校验，禁止 NaN/Infinity 且不许产出违协议 JSON；ComputeDocumentHash 输出 SHA256 十六进制）。
- 关键行为经探针实测后定稿：Newtonsoft 默认把 NaN 序列化成字符串 "NaN"（会静默改值），故设置 FloatFormatHandling.Symbol 使非有限数以裸符号产出并被全局拒绝；JToken.FromObject 不走转换器（TimeSpan 会物化为 TimeSpan 类型），Save 改用 JsonConvert.SerializeObject→JToken.Parse；裸 NaN/Infinity 字面量可被 JToken.Parse 读回为 Float，由 FindNonFiniteNumbers 拒绝。
- 错误码映射：$.schemaVersion 与 $.blocks[i].type 违例 → E_SCHEMA_VERSION（PRD 第 10 节原文）；其余协议违例（含 NaN/Infinity、未知属性、类型错误）→ T03 新增 E_SCHEMA_INVALID；诊断 details 记录 jsonPath。
- 测试（5a98455）：46/46 双运行时通过（原 4 + 新 42）。覆盖：有效 fixture 全字段语义；往返 DeepEquals 语义不丢（含 ²、break run、上下标 run、spacer slotCount）；未知扩展字段（extensions 命名空间 key，嵌套对象/布尔）原样保留；序列化确定性；documentHash 稳定且内容敏感；未知主版本/未知块类型 E_SCHEMA_VERSION 拒绝且不物化；缺字段/未知属性/错误枚举/负索引/类型错误/非对象根/损坏 JSON 拒绝；NaN/Infinity/-Infinity 在 extensions 与 layout measuredWidth 拒绝；Save 对含 NaN 模型与违 Schema 模型抛错；校验器关键字级用例（type 含整数浮点、enum/const、additionalProperties、pattern、minLength、minimum/maximum/exclusive、items/minItems、if/then/else/not、$ref、未知关键字忽略）；LayoutResult/RenderRequest 有效 fixture 往返（跨文件 $ref）与违例定位。
- 验证：scripts/build.ps1 -Target Core 与 -Target All 均 0 警告/0 错误；net8.0 与 net48 各 46/46。-Target All 覆盖 PluginHost 对 AutoCAD 2021（24.0）引用的完整解决方案编译。未运行 -Target Package：T03 无宿主可见变化，且打包脚本 ZIP 名仍硬编码 m0（阶段命名属后续打包任务）；未做真实 CAD 加载验证（AC-04 不需要宿主）。
- 边界遵守：核心无 AutoCAD/UI/Word COM 引用（既有架构守卫测试通过）；未开发 DOCX 解析、排版算法、CAD 输出、Future 端口；未标定生产值未填入（布局/渲染 fixture 为显式测试值，standardRef.version 保持 "pending"）。
- 提交：d90ae01、fdc8e55、2cc0306、7ddaa8e、03b91d4、6cbebe4、e06a13e、5a98455（git log --all --oneline --grep='(T03)'）。
- 未解决/后续：院标与 Layout Template 的 JSON Schema 及发布校验随 T06 标定落地；ResolvedStyle/FontEntry 细节（大字体、文件身份）待 T06/T07 实测校准；render-request 的跨文件 $ref 依赖 schemas 文件名稳定，重命名需同步三处。
- 下一任务：T04（DOCX 样本 → 段落/样式/诊断解析，AC-01～03）。
