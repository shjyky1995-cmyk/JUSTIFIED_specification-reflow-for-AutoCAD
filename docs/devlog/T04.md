# T04 — DOCX 解析（段落、样式、诊断）
## 2026-09-22：领取
- 输入：PRD 第 6 节、第 8 节 REQ-DOC-001/002、第 10 节错误码、第 12 节 AC-01～03；T03 已合入 main（d073798）。
- 输出：DocxAdapter 将只读 DOCX 解析为 Document 与诊断；脱敏样本由测试在内存中生成标准 OPC 包，不依赖 Word/WPS 安装。
- 范围：DocxAdapter、tests 中的解析测试与输入说明、docs/TASKS.md 与本日志。不改排版、测量、CAD 输出，不把解析接到插件命令（那是 T10）。
- 边界决策（不改共享契约）：
  - T03 冻结的 ParseProfile 只有 StandardRef，没有样式映射，院标包要到 T06 才落地。样式映射作为 DocxDocumentParser 的构造选项传入，不修改 Contracts，也不把未标定别名写死成生产默认值。
  - 正文样式必须出现在调用方给出的映射里才算正文。一级/二级标题允许沿 basedOn 继承；祖先是正文时，不把未映射样式（含三级标题）降级成正文。
  - 新增的 info 码放在 DocxAdapter（I_IGNORED_FORMAT、I_IGNORED_PAGINATION、I_IGNORED_HEADER_FOOTER、I_MACRO_NOT_EXECUTED），不扩 DiagnosticCodes。
  - 资源上限由调用方显式传入。CAL-09 仍是 pending，解析器不内置静默字节上限。
  - 自动编号的十进制单级/多级与重启随解析器一起实现，因为编号标签和正文必须在同一次解析里分开；非十进制格式返回 E_NUMBERING，不猜测。T05 仍负责更细的编号/空白审计样本。
- REQ/AC：REQ-DOC-001/002，对应 AC-01～03 的解析部分。合成包不能代替真实 Word/WPS 文件，AC-01 在 CAL-06 样本到达前不能标记通过。AC-03 的 CAD 分页与坐标要等排版任务，本任务只证明视觉格式不进入模型。
- Git 历史：git log --all --oneline --grep='(T04)'。

## 2026-09-22：解析实现与核心验证
- 改动：DocxAdapter 增加只读解析。`DocxDocumentParser` 实现 `IDocumentParser`；`DocxFileSource` / `DocxBytesSource` 提供文件与内存来源；样式映射、documentId、disciplineCode 和可选字节上限由 `DocxParseOptions` 传入。测试在内存中生成 OPC 包，并附 `tests/.../Fixtures/DOCX输入说明.md`。
- 行为：按 styleId、显示名和标题的 basedOn 链识别一级/二级标题；正文必须出现在映射中，基于正文的三级标题不会降级成正文。手工编号留在原文。十进制自动编号生成独立 label，支持多级、startOverride、新列表和 lvlRestart=0；其他格式返回 E_NUMBERING。空段保留且不合并，最后一个空段视为编辑器终止段并丢弃。显式换行保留为独立 `\n` run；分页符、分栏符、分节符和上次渲染的自动分页不进入正文。上下标保留语义，字面 ² 不替换。表格、图片、文本框、公式、脚注、尾注、批注、嵌入对象、修订、域、制表符和未映射样式返回定位错误且不产出 Document。页眉页脚不进正文。宏不执行。成功结果可通过既有 JSON Schema。
- 检查：`scripts/build.ps1 -Target Core` 与 `-Target All` 均为 0 警告 / 0 错误。net8.0 与 net48 各 68/68（原 46 + 新 22）。All 同时编译了 AutoCAD 2021 插件工程。未做真实 Word、WPS 或 CAD 加载。
- 未通过、因此不标完成、不合入 main：
  - AC-01 需要同一内容的真实 Word 与 WPS 文件。CAL-06 仍是 pending，合成包只证明解析器能区分 styleId 和显示名。
  - AC-03 的 CAD 分页、坐标和字高要等排版与院标。本任务只证明字体、字号、颜色、段距和页边距不进入模型，标题与正文之间不多出一个空槽。
  - 解析还没有接到插件命令，CAD 里还不能点选 DOCX。
- 需要调用方以后补上的口径：生产样式映射随 T06 院标包接入；CAL-09 给出源文件和解压上限前，不传上限就不做数值拦截。
- 下一任务：先用真实 Word/WPS 样本验收 T04。通过后可以做 T05 的更多编号与空白审计，或在 T06 把样式映射接到院标包。T05 的十进制编号、空段和硬换行已有基线实现与测试，不把 T05 标成完成。

## 2026-09-22：补充可打开的脱敏样本
- 原因：验收需要一份能用编辑器打开的 DOCX，而不是只存在于测试内存里的压缩包。
- 改动：生成「测试文件/示例-结构说明.docx」。内容为虚构的三层框架说明，含一级/二级标题、十进制多级编号、空行、硬换行、上下标、字面 ² 和工程符号；页眉页脚不进入正文。样式使用 Word 内置 Heading1、Heading2、Normal，并带中文别名。解析测试会读这份文件，并与重新生成的包比对语义。
- 本机没有 Microsoft Word。WPS Office 12.1 已安装在 E:\WPS Office\12.1.0.28505，但自动另存失败，没有产出 WPS 保存的第二份文件。
- 检查：`scripts/build.ps1 -Target Core` 为 0 警告 / 0 错误；net8.0 与 net48 各 69/69。未改插件，未再跑 CAD 打包。

## 2026-09-22：WPS 另存对照
- 输入：用户用 WPS 将样本另存为「测试文件/示例-结构说明-1.1.docx」。
- 结果：按显示名 heading 1 / heading 2 / Normal 映射后，两份文件的标题层级、正文、十进制编号、空段、硬换行、上下标和工程符号一致。WPS 把 styleId 改成了 1=Normal、2=heading 1、3=heading 2；若把数字 1 当成一级标题，正文会被误判成标题。
- 检查：对照测试纳入 Core 后，net8.0 与 net48 各 70/70，0 警告 / 0 错误。
- 状态仍是待验收：AC-03 的 CAD 分页和坐标还没有排版任务，不能把本对照写成整项 AC-01～03 完成。T05 仍单独做编号与空白的进一步审计。
