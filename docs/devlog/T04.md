# T04 — DOCX 解析（段落、样式、诊断）
## 2026-09-22：领取
- 输入：PRD 第 6 节、第 8 节 REQ-DOC-001/002、第 10 节错误码、第 12 节 AC-01～03；T03 已合入 main（d073798）。
- 输出：DocxAdapter 将只读 DOCX 解析为 Document 与诊断；脱敏样本由测试在内存中生成标准 OPC 包，不依赖 Word/WPS 安装。
- 范围：DocxAdapter、tests 中的解析测试与输入说明、docs/TASKS.md 与本日志。不改排版、测量、CAD 输出，不把解析接到插件命令（那是 T10）。
- 边界决策（不改共享契约）：
  - T03 冻结的 ParseProfile 只有 StandardRef，没有样式映射，院标包要到 T06 才落地。样式映射作为 DocxDocumentParser 的构造选项传入，不修改 Contracts，也不把未标定别名写死成生产默认值。
  - 正文样式必须出现在调用方给出的映射里才算正文。一级/二级标题允许沿 basedOn 继承；祖先是正文时，不把未映射样式（含三级标题）降级成正文。
  - 新增的 info 码放在 DocxAdapter（I_IGNORED_FORMAT、I_IGNORED_PAGINATION、I_IGNORED_HEADER_FOOTER、I_MACRO_NOT_EXECUTED），不扩 DiagnosticCodes。
  - 资源上限由调用方显式传入。CAL-09 仍是 pending，解析器不内置静默字节上限。
  - 自动编号的十进制单级/多级与重启随解析器一起实现，因为编号标签和正文必须在同一次解析里分开；非十进制格式返回 E_NUMBERING，不猜测。T05 仍负责更细的编号/空白审计样本。
- REQ/AC：REQ-DOC-001/002，对应 AC-01～03 的解析部分。合成包不能代替真实 Word/WPS 文件，AC-01 在 CAL-06 样本到达前不能标记通过。AC-03 的 CAD 分页与坐标要等排版任务，本任务只证明视觉格式不进入模型。
- Git 历史：git log --all --oneline --grep='(T04)'。
