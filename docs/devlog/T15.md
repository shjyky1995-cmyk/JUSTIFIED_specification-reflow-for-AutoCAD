# T15 桌面端框架与路线讨论日志

## 2026-09-26：D0 开始，仅形成可评审方案

- 输入：用户选择首批场景“离线导入工作台”；用户要求先把整体技术框架和开发路线定好，再开始桌面功能开发，并选择先比较 Electron 与 .NET。
- 文档结构：保留已签收的 CAD V1 根 PRD，另建 `docs/DESKTOP_WORKBENCH_PRD_V1.md` 记录桌面目标、边界、技术比较、交接方案、D0～D3 路线；根 PRD、AGENTS、ARCHITECTURE、TASKS 和 NEXT_AGENT 提供入口。草案标明待确认，不把候选技术写成已定。
- 比较依据：现有核心 `netstandard2.0`、CAD 插件 `net48`，桌面目标为 Windows 离线；Microsoft 官方 WPF、.NET Standard、.NET 支持政策和单文件发布资料，以及 Electron 官方进程模型、安全、分发资料。结论为当前项目约束下推荐 WPF + .NET 10 LTS，属于方案推论，未做包体/启动/内存实测。
- 初版关键业务边界当时待用户进一步确认；下一条记录修正。
- 本次只改方案文档，未新增桌面源码、NuGet/npm 依赖或运行服务。检查：链接、任务状态与 Git diff；用户确认三项决定后才进入 D1。

## 2026-09-26：按用户答复收敛为单 DOCX 流程

- 用户确认固定模板填写生成 DOCX，在 Word/WPS 中修改；CAD 用现有 `DSS` 直接导入这份最终 DOCX。删除草案中的本机交接请求与文件哈希确认，不新增两端共享协议。
- 用户说明文字直接落在一张张图纸内，界面不需要“说明页数”。此前提出的“页数待 CAD 计算”是错误的产品表述；内部图幅续排计数不作为桌面 UI 字段。
- 重新查看 `测试结果/0925/测试T14/` 的 8 张 PDF 与 3 张截图，旧输出确有文字进入顶部图框、越过右侧、续图裁切。现行 1.1.0 模板的左上锚点、顶部 60mm 留白与页宽步长是对此的后续修复，但没有修复后真实 CAD 打印 PDF，不能声称该版打印问题已经实测消除。CAD 打印复核单独接续。
- 仍待用户讨论 WPF 与 Electron 的技术选型；D0 未签收，不开始桌面编码。

## 2026-09-26：D0 技术路线定稿

- 用户在 WPF 与 Electron 对比后明确更重视“炫酷”的前端，并把技术判断交给 Agent。决定 Electron + React/TypeScript 负责桌面界面，.NET 10 本机工作进程负责 DOCX 写出和检查；主要代价是多一套构建/运行环境及更大的包，D1/D3 实测外观、资源与部署成本。
- 用户再次确认桌面与 CAD 是两个独立程序：桌面生成普通 DOCX，Word/WPS 修改，用户在 CAD 重新选这个文件导入；不得发展自动同步、后台连接或共同服务。Electron ↔ .NET 通信仅为桌面内部实现。
- 正式记录为 `docs/DESKTOP_WORKBENCH_PRD_V1.md` 和 ARCHITECTURE ADR-013，从根 PRD、AGENTS、TASKS 和 NEXT_AGENT 链接。D0 完成；下一步进入 D1 开发。0925 旧 PDF 已核对，新版打印由用户测试后反馈，不作为当前桌面工作的阻塞。
