# 当前接续状态（所有 Agent 共用）

更新：2026-09-22。固定行为见WORKFLOW.md；本文件是唯一当前接续入口，不是新的产品范围。

## 当前状态

- 本轮小任务：T06-WF，完善固定开发/验收/跨Agent接续流程，已完成文档检查；无需CAD操作。
- 阶段：T06待验收；T07待开始，用户已授权其先行，完整打印验收后移。
- 当前分支：task/T06-standards-measure。上一业务交接提交73b9366；本次流程提交通过 `git log -1 --oneline` 查询。接手先检查git status，不能把本文件替代工作区检查。
- 用户待办：无。下一条“继续下一步”直接触发下述T07-01，不能再要求用户重述需求或先验收打印。
- 当前交付：AGENTS.md、WORKFLOW.md、DEVELOPMENT.md、TASKS.md及本文件。自动文档检查通过不等于T06图面验收通过。

## 唯一下一小任务：T07-01 基础自动换行

目标：在既有LayoutEngine模块实现可独立测试的普通文本换行，连接既有ITextMeasureService；按实际测量寻找能容纳的最长合法前缀，遇无法容纳单个单元时有限退出并诊断。此小步不做CAD UI、分页或打印。

1. 按AGENTS/WORKFLOW读入口、架构、PRD第6节换行规则、现有LayoutEngine/Contracts接口和核心测试；复用T04/T05 DocxAdapter，不重写Word解析。领取T07-01，创建docs/devlog/T07.md并更新TASKS为进行中。
2. 默认串行，在当前已提交HEAD上创建task/T07-line-break（已存在则先核对，不覆盖），明确记录继承未验收T06的依赖；不合main，不开子Agent。
3. 允许范围：LayoutEngine、对应核心测试、任务/日志/接续文件。保持Contracts不变；确需共享协议变更才按WORKFLOW讨论，内部类和必要测试自主完成。
4. 第一小步至少覆盖：中文/英文混排、恰好容纳/稍超宽、基本行首行尾禁则、显式换行、空输入、单个不可容纳单元诊断；不得按字符数估宽。对测量失败保持诊断，不静默删字。
5. 检查现有契约后确定内部方法，不预造Future空服务。此步未覆盖的编号前缀绑定、复杂token、空格细化明确留给T07后续小步，不宣称T07全部完成。完整规则仍要求“编号+两个U+0020+正文”、编号不孤立/不重复、不改正文内部语义。
6. 执行相关核心测试（适配既有net8.0/net48要求），提交可验证结果；更新NEXT_AGENT为真实状态和唯一后续小步，按固定模板交付。纯算法这步无需用户打开CAD。

输入资料：docs/TEXT_FORMAT_V1.md、docs/PAPER_TEMPLATES_V1.md、standards/drafts。格式4.5/0.75/7.2，A1/A2三栏、A3两栏，零悬挂。草案不得改classification绕过发布；算法测试通过既有接口注入明确标注的测试配置/测量服务，保留生产缺值阻断。

## 保留证据与延期项

- T04/T05 Word段落/样式/编号解析已完成；T06已有HostTextMeasureService，后续适配复用。阶段顺序T07→T08→T09→T10，先可演示闭环，再补完整发布验证。
- local/calibration/font-test-20260922-082232/T06-font-evidence.dwg及evidence.json：30个真实AutoCAD DBText，高4.5/宽0.75，读取边界；非插件测量链路验收，也未验证上下标。真实正文仅本地，不入库。
- PDF未生成：COM在窗口尚未设置时设PlotType=4报输入无效，暂停此支线。未来再做时先设置合法窗口，不重复新建测试图；不得称打印已通过。
- 原业务图未改：680对象、DBMOD=0、安全配置未变。若另机缺local资料，只记录缺失，不凭空重造旧证据；不影响无需该资料的核心算法开发。
- T06完整字体/符号/上下标/测量公差/图面/打印、T11/T12正式发布验证保留；本次开发先行授权不等于通过这些项目。
