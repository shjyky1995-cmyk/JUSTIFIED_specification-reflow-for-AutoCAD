# 当前接续状态（所有 Agent 共用）

## 2026-09-25 最新接续（优先于下方历史）

用户回传 `测试结果/0925/YMFY.png`，要求缩小导入窗口与字、允许拖动，免选模板，并恢复 AutoCAD 新建图纸的窗口。`NotePickerForm` 已缩为 800×550、页眉可拖、内置 Logo 保留、日常只选 DOCX/图幅/单位比例；模板从安装包固定路径自动加载，无可用模板时只提示管理员。此为 T14 当前分支候选，未完成真实窗口验收。本机验证：Release 编译 0 警告，net8/net48 各 144/144，bundle 22 文件通过；离线预览目视无遮挡。已部署到当前用户 ApplicationPlugins，安装包内 `standards/published` 另放 `LOCAL-TEST-ONLY` 三图幅测试资料，仅供本机测试，不属于仓库的生产模板。旧包备份 `.bundle.backup-20260925-161538`。

CAD 会话读取到 `FILEDIA=1`、`CMDDIA=1`、`STARTUP=3`；通过 CAD 命令把 `STARTUP` 改为 `1`，当场回读三值均为 1。新插件部署后已重新启动 CAD，但电脑界面工具返回空应用列表且 COM 启动注册暂不可用，**重启后的对话框效果尚未直接观察**。唯一用户待办：按 `docs/T14_HOME_TEST.md` 复测“新建”弹窗、`DN_NOTE` 小窗口/拖动/免选模板、`N/mm²`，回传窗口截图、`DN_NOTE_OK` 与 F2 计时行；失败先修。原有夹具换行差异及 `测试结果/` 保持未暂存。停止 Google Drive 上传。T12 打印、断网、专业签收及 T13 安装器仍 pending；下方较早的 Drive 与选模板说明只作历史。

更新：2026-09-25。阶段3 T12/T14 进行中；从已合入 T01～T11 的 main（2bb89ed）建立 `task/T12-acceptance`，再派生当前工作分支 `task/T14-note-picker`。mvp-1.0 是候选闭环标签，不是正式发布。用户已回家，要求停止 Google Drive 上传并在本机测试；V2 CAD 窗口已改成 WinForms 候选，通过本机构建与双框架核心检查，真实 CAD 窗口观察待用户完成。外机旧版同图三图幅生成已取得证据；打印、断网、性能和专业签收仍待验。

## 2026-09-25 本机测试接续

用户 0925 回传指出窗口比例/留白/遮挡、缺本人 logo，并补充平方上标 `²` 显示问号。修复提交 `c3921cd` 后，用户确认日常窗口改为 Word DOCX、图幅、单位比例三项，模板版本由管理员维护（ADR-012）；三项提交 `92ead49`。新候选已通过 0 警告、net8/net48 各 144/144、bundle 22 文件核验，并于确认无 `acad` 进程后部署到当前用户 `%APPDATA%/Autodesk/ApplicationPlugins/JUSTIFIED_specification-reflow-for-AutoCAD.bundle`。旧候选备份为 `.bundle.backup-20260925-1549`；ZIP SHA256 和 DLL SHA256 见 T14 日志。**当前唯一用户待办**：重启 AutoCAD 2021，在空白测试图核对三项窗口、logo、单位比例、`N/mm²` 字形、取消与 F2 命令结果，按 `docs/T14_HOME_TEST.md` 回传。真实宿主修复仍 pending；成功后继续 T12 剩余验收及 T14 不同 DOCX/点位取消，随后 T13 图形化安装。表格为后续范围，仍不支持 V1；`tests/.../Fixtures/test-note-standard.json` 原有换行差异及 `测试结果/` 用户资料不得覆盖或入库。

2026-09-24 新需求及分支：用户明确同一 DWG 可先后插入不同图幅/版本/DOCX，要求每次在 CAD 小窗口选择。`task/T14-note-picker` 从 `task/T12-acceptance` 派生，不能绕过未验收祖先直接合 main。T14 的 WinForms 入口已接入 `DN_NOTE`；`DN_NOTE_REPEAT` 重用最后一次选择；`DN_NOTE_SET` 仍供旧图/验收。单位电脑已实测同图连续 A1/A2/A3 与窗口取消；外观改版、不同 DOCX 等仍 pending。原 T12 私有转移 ZIP 是旧入口，不含 T14。

2026-09-24 新用户要求：推广时普通设计人员须从单一发布包经图形化入口安装，重启 AutoCAD 后使用；不得要求 GitHub、PowerShell、手工复制 `.bundle` 或人工校验哈希。PRD 3.3/NFR-006 已登记，T13 跟踪安装、升级、卸载与全新电脑实测。当前 T12 私有 ZIP 和手工步骤仅供开发验收，不能包装为正式用户体验；正式发布还须完成 T13，且不能把 T12 的 local-test-only 标准装进生产包。用户随后明确：先完成全部 CAD 功能与安装流程，CAD 窗口视觉改版可后做；两个端口（独立桌面应用与 CAD 插件）将来统一设计风格，桌面端倾向 Electron。现阶段只记录后续提案，不实施桌面技术栈变化，详见 ARCHITECTURE/TASKS。

## 已完成与证据

- 正式入口 `DN_NOTE_SET` 设一次标准/图幅/单位/DOCX，`DN_NOTE` 点一次位置；T11 已完成候选包、限额、报告、撤销与处理中取消宿主检查。证据见 T06/T10/T11 日志。
- T12 已取得 WPS 工程说明 A1 宿主证据：2 页、219 个 DBText、字形与续页核对；CAL-06 Word/WPS 输入语义完成，见 `docs/devlog/T12.md`。独立复核修正了旧日志的字符数：同 SHA 的原始 w:t 为 9080 个 UTF-16 单元，实体显示文本为 9076；仅 4 个自然换行边界可折叠空格，源映射保留。旧“8863 完全一致”不再作为证据。
- 用户单位电脑回传 `测试结果/0924`（本地业务证据，忽略入库）：候选 `bc3cb48` 的 AutoCAD 命令输出显示 `DN_DIAG_OK`；同图 A1 2页/219对象、A2 2页/262对象、A3 5页/255对象，合计736对象，三次零警告；第四次取消窗口显示 `DN_NOTE_CANCELLED`。截图仅含生成图面，不含选择窗口。该证据支持 T14 混合图幅基本闭环，不证明不同 DOCX、打印、断网、速度或专业图面核对。
- 用户反馈选择窗口外观差、点位后生成慢。新代码已加入 `DN_NOTE_TIMING_MS` 分段计时，编译0警告、net8.0/net48各143/143；旧包没有计时，不能推算秒数。优先获取新包真实耗时，再优化主耗时；按用户最新排序，CAD 窗口视觉改版在功能与安装流程闭环之后、普通用户正式推广之前完成。
- 用户确认 A2 从点下位置至全部生成约 21 秒；插件版本及分段尚待确认，暂不当作正式 P95 性能标定。该量级已构成实际体验问题，正式推广前必须优化并外机复测。
- 第二批 `测试结果/0924(1)` 命令行已给出分段：A1/A2/A3 分别约 28.23/27.96/27.56 秒，每次约 8000 次 CAD 字宽测量，占排版耗时 99% 以上。该目录的 BUILD.json 仍是旧 `bc3cb48`，不能用它证实本次包精确版本。`有问题.png` 标出上标/工程符号问号；源 DOCX 有 `m³`、`10⁻⁴`，须区分实体字符串和字体显示问题。详见 T12 日志。
- 用户请求并已制作 CAD 选择窗口视觉概念 `docs/assets/cad-note-picker-concept-v1.png`：参考其暂定三段式 logo，只用近似图形、不锁定品牌名称；少字、图标配短标签、图幅卡片、DOCX/模板/单位/标准包清晰可选，详情见 TASKS。此为设计稿，未改 WinForms 代码。
- 用户更正视觉稿后，最新设计为 `docs/assets/cad-note-picker-concept-v2.png`：增加“更多”图幅入口、移除日常“标准包”、仅四项输入与“选择→点位置”两步。V1 留历史。CAD 代码尚未实现 V2；按用户意见先解决性能与字形。
- 性能与字形候选已提交 `43fa507`：同次生成复用一组未提交 CAD 测量事务和相同样式；当前 `tssdeng.shx` 下测量与落图一致把用户确认的 `φ/Φ` 编码为 `%%132`；字面 `³/⁻/⁴` 用现有已标定上标段表达，`²` 保留。0 警告、net8/net48 各 144/144、候选 bundle 22 文件核验通过。私有复测包 `artifacts/t14-share/CAD修复复测包-43fa507.zip`，SHA256 `AC83E2EE44020937C7DDDB5F2693A1958DFABCCD549D3B589147751DCEDE6251`，已上传用户本人 Drive 文件 ID `10RsfKPmTuVQddU6mgiNH_JSuDdDUJ6yk`；云端回读 ZIP 大小 2,209,535 字节。尚无该候选的真实宿主性能和字形证据。
- 计时版私有复测包：`artifacts/t14-share/T14-CAD计时复测包-383d476.zip`，最终 SHA256 `DC61CBEAAB93C1E953E1B369E1CA04800744514AB294C47F4C26F85C4DB67FF2`；包含业务样本和 44 行详细小白指南，仅供授权测试电脑使用。源码提交 `383d476`，候选插件来源该提交；包重新解压后 8 文件核验通过。
- 上述计时包已上传并就地更新用户本人 Google Drive 文件 ID `1iDvzVPj6WsmCSmdEGpbh2px_muoiIbHq`，回读确认最终 ZIP 文件名与大小 2,209,247 字节；没有发送邮件，也未设为公开共享。Drive 链接可供本人账号下载。
- 本轮用项目真实 DocxAdapter 扫描用户 13 份原始说明：4 份无表格可解析，9 份含表格按 V1 拒绝。原件不改、不入库；本机报告在 `artifacts/t12-paper/20260924-074811-649/source-inventory.json`。
- 本机三图幅测试包与同一份无表格 WPS 样本已准备：`artifacts/t12-paper/20260924-074811-649`。样本 SHA256 `3D6402EC462A5649341718B3A5597C39BDF6F519D8D4FD92EF0D250EEFA10E98`；标准及 A1/A2/A3 模板均通过正式加载校验，外层清单标 local-test-only/releaseAccepted=false。`standards/published` 仍为空，不能把测试包当发布资产。步骤在 `docs/T12_ACCEPTANCE.md`。
- 外机/断网材料已装成私有本机转移包 `artifacts/t12-transfer/20260924-080328-947.zip`，ZIP SHA256 `E7848DED77D078BD625D8BC9478452C3DF6644726F88A903A09A944583CB1209`；解压后清单校验 9 文件通过，候选插件内部 22 文件校验通过。包内标准目录另有 LOCAL-TEST-ONLY 标识。含用户业务样本，仅在其授权设备内使用，外机运行本身仍 pending。

## 用户待办与唯一恢复动作

计时版候选包已在用户本人 Drive，第二批计时、窗口截图和 logo 草图已回传。Agent 下一步先修宿主字宽测量次数/事务开销，并核查 `m³`/`10⁻⁴` 问号的产生位置；用户可继续提供 DBText 属性证据。不同 DOCX/点位取消、打印、断网和专业图面核对按 T12/T14 继续。宿主/桌面默认由用户操作；仅用户明确不方便并要求代操作时才用 Computer Use。

## 仍待完成

- 三图幅已在单位电脑同图生成，但无计时报告、独立空白图正式核对及专业图面复核；本地测试包不得提前放入 `standards/published`。
- 外机安装和字体身份、断网运行、一次真实打印、CAL-08 取消时延 P95 与真实业务性能、CAL-09 源文件/解压/页数/对象上限标定、结构专业签收与成效原始记录。缺证据保持 pending。
- T13 普通用户图形化安装包及全新电脑安装/升级/卸载验收尚未开始，不能宣称已有“一键安装”。
- T14 同图 A1/A2/A3、窗口取消有宿主命令记录；不同 DOCX、点位取消仍待功能验收。窗口视觉改版按用户意愿推后至 CAD 功能与安装流程闭环后，但普通用户正式推广前须完成。`standards/published` 仍为空，不能把本地测试包视为发布资产。
- 当前工作区原有 `tests/.../Fixtures/test-note-standard.json` 末行换行差异保留，不暂存。业务源文件、WPS 副本和截图为用户资料，已补忽略规则；仅提交脱敏工具与记录。

## 分支与交付

T14 最新独立候选包：`artifacts/packages/20260924-084110-671/JUSTIFIED_specification-reflow-for-AutoCAD-0.1.0-candidate-bc3cb48.zip`，SHA256 `D368945B1E898895A7449F9231C1EF268123B07D886FDA3759B4E8F8D6CE2B84`；0 警告、双框架各 143/143、bundle 22 文件校验通过，BUILD.json 已如实列出 T12/T14/T13 待验项。前两个 T14 ZIP 留历史，不作为当前验证包；原 T12 私有转移 ZIP 不含新界面。

面向用户复制的组合包是本机私有文件 `artifacts/t14-share/T14-CAD选择窗口-测试包.zip`，SHA256 `8491E9B210334388034691EE1FCACF4CCB9E0B75E064367508F47B3D9CF05DB9`；包含用户已授权携带的业务样本、T14 候选插件和本地测试标准，重新解压后 8 文件校验通过。**尚未发邮件**：当前无 Gmail 连接，连接请求未验证成功；Gmail 官方会阻止含 `.dll`/`.ps1` 的 ZIP 附件。不能将本机 ZIP 说成已上传或送达。

当前唯一恢复动作：用户按新 Drive 私有包内《先读我.txt》在单位电脑复测 A2 点位后时间、`%%132` 钢筋字形、`m³` 与 `10⁻⁴`、分栏和一次撤销，回传计时行与问题放大图；Agent 据真实结果先修失败。CAD 窗口 V2 视觉稿已保存，功能与安装流程闭环后再落地，普通用户正式推广前验收。桌面 Electron 仍为后续提案。宿主/桌面默认由用户操作；仅用户明确不方便并要求代操作时使用 Computer Use。
