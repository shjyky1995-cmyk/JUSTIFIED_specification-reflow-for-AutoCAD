# 当前接续状态（所有 Agent 共用）

更新：2026-09-22。执行WORKFLOW定稿：按大阶段连续推进，不按微小步骤交还用户。

## 当前状态

- 流程定稿已完成；用户准备交给另一Agent开发，本轮不启动实现。
- 下一交付阶段：**阶段1 排版引擎，完整推进T07+T08**。T06仍待验收，用户已授权先行开发；不要重新等待完整院标/打印。
- 当前分支task/T06-standards-measure；从接手时已提交HEAD为基础，先检查git status及git log。当前流程提交可通过git log查询，无未提交业务代码。
- 用户待办：无需操作CAD或补资料。用户说“开始/继续下一步”或要求按交接开工，直接执行本阶段。

## 下一Agent执行目标：完成排版引擎阶段

1. 按AGENTS读入口、PRD第6节、接口和已有测试。复用T04/T05 DocxAdapter及T06测量基础，不重写解析器。先记录T07领取并建立task/T07-line-break；分支已存在时核对恢复，不能覆盖。
2. 完整实现T07：真实测量接口驱动的文本单元/禁则换行；中文英文及工程token边界；编号+两个半角空格内联、编号不孤立/不重复；空格与显式换行语义；超宽单元/长token、测量失败和取消诊断，确保有限退出。无需把这些拆成多轮用户确认。
3. T07相关检查通过并提交后，按项目规则建立/接续T08分支，直接实现T08：固定行槽、按当前栏宽换行、不同栏宽、空行、跨栏跨页、无多余空尾页。按现有接口合理组织T07/T08，避免先按首栏宽切完全文。
4. 阶段范围：LayoutEngine、对应核心测试、必要配置/文档记录；不私改共享Contracts。普通内部实现自行决定；确需共享协议或架构变更才集中说明并讨论。默认串行，不开子Agent。
5. 使用已定格式和三模板验证：4.5 mm字高、0.75宽度、7.2 mm行距，A1/A2三栏、A3两栏，零悬挂；所有数值来自配置，不写死。资料见TEXT_FORMAT_V1.md、PAPER_TEMPLATES_V1.md及standards/drafts。
6. 验收至少包含T07的中文临界、混排、禁则、编号/空白、不可容纳诊断及T08的6/7行边界、跨栏/跨页、不同栏宽、空行、刚好满页不出空尾页；执行相应net8.0/net48核心测试。按照PRD既有验收点补必要覆盖，不能只测简单样例。
7. 生产草案不得改classification绕过发布，不能给pending字段编值。核心测试通过现有接口注入明确测试配置/测量；保留正式缺值阻断。真实测量若出现会影响排版的实际问题应修复或说明，不把全部字体打印任务重新当作本阶段先决条件。
8. 正常情况下持续到T07/T08阶段成果完整再交付；内部每个可验证成果仍正常提交。遇真正阻塞、必要用户操作、重大决策或额度不足才checkpoint并更新本文件。不得做完基础换行就要求用户说继续。
9. 本阶段完成后用户无需CAD手工操作，按WORKFLOW模板报告测试/提交。下一交付阶段设为阶段2（T09+T10）：DBText落图及Word→CAD操作闭环，不在本阶段冒称已完成CAD输出。继承未验收T06的分支先保留，不把祖先一并合main。

## 保留证据与延期项

- T04/T05 Word段落/样式/编号解析已完成；T06已有HostTextMeasureService，后续适配复用。阶段顺序T07→T08→T09→T10，先可演示闭环，再补完整发布验证。
- local/calibration/font-test-20260922-082232/T06-font-evidence.dwg及evidence.json：30个真实AutoCAD DBText，高4.5/宽0.75，读取边界；非插件测量链路验收，也未验证上下标。真实正文仅本地，不入库。
- PDF未生成：COM在窗口尚未设置时设PlotType=4报输入无效，暂停此支线。未来再做时先设置合法窗口，不重复新建测试图；不得称打印已通过。
- 原业务图未改：680对象、DBMOD=0、安全配置未变。若另机缺local资料，只记录缺失，不凭空重造旧证据；不影响无需该资料的核心算法开发。
- T06完整字体/符号/上下标/测量公差/图面/打印、T11/T12正式发布验证保留；本次开发先行授权不等于通过这些项目。
