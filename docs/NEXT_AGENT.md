# 当前接续状态（所有 Agent 共用）

更新：2026-09-26。阶段3 的 T12、T14、T13 已收尾；`main` 已按原提交顺序快进合并至 `467999a`。用户明确将本人此前检查的 T12/T13 项目签收通过，并免除缺少独立记录的逐页打印清单、≥20 次性能 P95、全新电脑安装与回滚演练作为本阶段关卡。**不要把免除项写成已实测。** 取消时延 P95 此前已由用户移出范围。

## 本阶段成果

- AutoCAD 2021 中的 `DSS`：每次选择 DOCX、A1/A2/A3 图幅和单位比例，点一次图面左上角生成 DBText；同图可混用，模板由安装包维护且每图幅只保留唯一有效版本。
- T12 有三图幅生成、打印 PDF、断网运行等用户反馈；T14 不同 DOCX、点位取消有宿主记录；T13 真实位置安装、升级、卸载有用户截图和沙箱自动检查。证据边界见 `docs/T12_SIGNOFF.md`、`docs/T13_HOME_TEST.md`、任务日志。
- 本轮界面由 700×400 调为 760×440，修复卡片文字出界和圆角深色残影，维持 Noto Sans SC 与原配色。真实 WinForms 预览：`docs/assets/cad-note-picker-v8-balanced.png`；新版窗口未单独取得 CAD 宿主截图。
- 2026-09-26 `build.ps1 -Target All`：零警告，net8/net48 各 164/164。正式包 BUILD.json 含用户免除项和源码 SHA；以最终 `Package` 构建与校验输出为准。

## 仓库与用户资料

- 唯一远端为 GitHub `origin`；用户所说“GitMe”是口误。只推送现有 GitHub 私有仓库。
- 原工作目录 `task/T13-installer` 有用户资料 `测试结果/`、测试 DOCX 和测试夹具末行换行差异，均未暂存、未覆盖。发布构建在 `artifacts/release-worktree` 的干净 `main` 检出中完成。
- CAD 宿主与桌面操作按 AGENTS 规则由用户进行；本轮无额外用户待办。

## 唯一下一大阶段

桌面端开发。现有 PRD 明确独立桌面工作台属于 V1 后续能力，Electron 仅为候选。先与用户确定首批桌面业务场景与交互边界，做最小离线原型并记录包体、启动、安装及与 C# 核心通信的比较，再由用户确认技术栈与共享接口后实施；不要预建空服务或擅改 CAD 已验证闭环。
