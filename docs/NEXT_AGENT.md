# 下一位 Agent：优先打通 Word 到 CAD

用户最新要求：额度有限，优先功能开发，停止把完整字体/打印验收作为自动排版开发的前置。无需再索取完整院标或重复询问格式。

1. 先检查Git状态，读TASKS/ARCHITECTURE及最新T06日志。当前分支task/T06-standards-measure；T06未验收，不合main。新工作依规则建立task/T07-line-break分支，以当前已提交状态为基础，不丢弃已有实现；串行工作，不开子Agent，不重写历史。
2. T04/T05已有DocxAdapter和解析测试，先复用；不要重写Word解析器。T06有HostTextMeasureService真实字体边界测量。检查现有接口后首先实现T07 LayoutEngine：文本单元、编号+两个半角空格的内联前缀、真实宽度、禁则换行、长token诊断；编号不独占一行、不重复编号、不改正文。
3. 按任务边界分步提交，再衔接T08三图幅分栏/续页，T09 DBText落图，T10串起DOCX选择到CAD输出。不要继续只写方案文档。每步先完成最小有效代码和针对性测试；无需每次重跑无关测试。
4. 规则文件：docs/TEXT_FORMAT_V1.md、docs/PAPER_TEMPLATES_V1.md；草案在standards/drafts。字高4.5、宽0.75、行距7.2，A1/A2三栏、A3两栏。多套个人标准独立ID/版本/模板引用，数值不写死。单位转换显式指定，不能把无单位旧图默认为毫米。
5. 草案不是发布包：不得修改classification来绕过发布校验，也不得给pending公差/符号映射随便填值。T07核心测试按已有接口注入明确标注的测试样式/测量服务；真实字体测试通过独立测试入口使用已定参数。保持生产缺项阻断，避免借延期验收造成假发布。
6. T06已有新增证据：local/calibration/font-test-20260922-082232/T06-font-evidence.dwg及evidence.json，共30个真实AutoCAD DBText（5条合成字符/编号候选+25条真实文字），实际高4.5/宽0.75，已读取边界。它没有调用插件HostTextMeasureService，不能称插件链路测量通过；上下标候选仅Unicode字符串，不能称已实现分段上下标。
7. PDF尚未生成：COM设置PlotType=4时“输入无效”（尚未设置窗口），已停止这条支线并保存新建测试DWG。以后需要打印时先设置合法窗口再选window类型；勿重复创建大量测试图。原图未改，680对象、DBMOD=0、SECURELOAD=1、TRUSTEDPATHS空。
8. 完整特殊字符、上下标、真实打印和模板图面验收延至最小Word→CAD链路可演示后完成，正式发布前仍必做。T06状态仍待验收，T07获准先行实现；顺序调整是用户授权的阶段安排，不是删除验收要求。

用户现在无需操作或再给文件。下一次应得到T07代码/测试进展，后续得到可实际演示的DOCX→CAD操作入口；尚未实现时不要给用户虚构命令。每次结束明确完成/未完成、提交号、下一步是谁做，人工步骤必须具体。额度不够时先提交checkpoint和交接再停。
