# 当前接续状态（所有 Agent 共用）

更新：2026-09-22。T07 换行已提交并通过核心检查。本阶段尚未交付，继续 T08，不要交还用户。

## 当前状态

- 阶段1 排版引擎进行中。T07 待验收，T08 进行中。T06 仍待验收，未合入 main。
- 分支 task/T07-line-break。T07 提交用 `git log --grep="(T07)"` 查询。工作区应干净；若有未提交改动，先看是否属于 T08，不要覆盖。
- 用户待办：无。不要要求用户操作 CAD 或补资料。

## 下一动作：完成 T08 后按阶段模板交付

1. 从当前 T07 HEAD 建立 task/T08-column-flow。已存在则核对恢复，不覆盖。
2. 固定行槽、按当前栏宽换行、不同栏宽、空行、跨栏跨页、刚好满页不出空尾页。引擎已按栏宽逐行排，不要改回先切完全文。
3. 验收至少包括 6/7 行边界、跨栏/跨页、不同栏宽、空行、满页无空尾页。用三套草案几何（A1/A2 三栏、A3 两栏，4.5 mm、0.75、7.2 mm、零悬挂），数值从配置读取。草案文件保持 pending / uncalibrated，测试只在内存注入。
4. 跑 net8.0 与 net48 核心测试。通过后提交，更新本文件为阶段交付：用户无需 CAD 操作，下一阶段是 T09+T10。不要把 T07/T08 合入 main，也不要宣称 CAD 已输出。

## 保留证据与延期项

- T07 已覆盖中文临界、混排、禁则、编号/空白、长 token 警告、单字过宽和测量失败/取消。真实 SHX 行宽、上下标缩放和打印仍未验证。
- local/calibration/font-test-20260922-082232/T06-font-evidence.dwg 及 evidence.json：30 个真实 DBText，不是本阶段排版验收。PDF 未生成。原业务图未改。
- T06 完整字体/符号/上下标/测量公差/图面/打印、T11/T12 正式发布验证保留。
