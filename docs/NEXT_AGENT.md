# 当前接续状态（所有 Agent 共用）

更新：2026-09-22。阶段1 排版引擎已交付，等待用户一句“继续下一步”。不要把本阶段合入 main。

## 当前状态

- 阶段1 完成代码检查，T07、T08 均为待验收。T06 仍待验收，未合入 main。
- 分支 task/T08-column-flow，基线包含 T07 `29b9e39`。T08 提交用 `git log --grep="(T08)"` 查询。
- 用户待办：无需操作。没有 CAD 命令、没有落图。回复“继续下一步”即进入阶段2。

## 本阶段结果

- `SpecificationLayoutEngine`：测量换行、内联编号、固定行槽、按栏宽续排、跨栏跨页、满页无空尾页。
- 核心测试 `LineBreakTests`、`ColumnFlowTests`。三套草案几何只在内存中注入可测状态，草案文件仍是 pending / uncalibrated。
- 未做：DBText 落图、命令入口、真实 SHX 行宽、上下标缩放、打印。不得宣称 CAD 已输出。

## 下一阶段：T09+T10 Word→CAD 闭环

用户说“继续下一步”后，从本 HEAD 建立 task/T09-dbtext，复用现有解析和本引擎，实现 DBText 落图，再做 T10 操作闭环。仍不把未验收的 T06/T07/T08 直接合入 main。

## 保留证据与延期项

- T06 字体/符号/上下标/测量公差/图面/打印仍待验收。local 证据图不是本阶段验收。PDF 未生成。原业务图未改。
- 页数上限目前只是引擎失控保护（默认 10000），不是已标定的 CAL-09。
- T11/T12 正式发布验证保留。
